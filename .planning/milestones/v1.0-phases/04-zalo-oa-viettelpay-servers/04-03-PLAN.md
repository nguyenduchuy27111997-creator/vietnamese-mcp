---
phase: 04-zalo-oa-viettelpay-servers
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - servers/mcp-momo-vn/CLAUDE.md
  - servers/mcp-momo-vn/README.md
  - servers/mcp-zalopay-vn/CLAUDE.md
  - servers/mcp-zalopay-vn/README.md
  - servers/mcp-vnpay/CLAUDE.md
  - servers/mcp-vnpay/README.md
  - servers/mcp-zalo-oa/CLAUDE.md
  - servers/mcp-zalo-oa/README.md
  - servers/mcp-viettel-pay/CLAUDE.md
  - servers/mcp-viettel-pay/README.md
  - README.md
  - .mcp.json
autonomous: true
requirements:
  - INFRA-07
  - INFRA-08
  - INFRA-09

must_haves:
  truths:
    - "Every server directory has a CLAUDE.md with tool catalog, env vars, mock mode, and workflows"
    - "Every server directory has a README.md with quick start and full tool reference"
    - "Root README.md presents the project as the first Vietnamese MCP server collection with all 5 servers listed"
    - ".mcp.json has all 5 server entries with correct env vars"
    - "npm test from repo root runs all 5 servers' tests and they all pass"
  artifacts:
    - path: "servers/mcp-momo-vn/CLAUDE.md"
      provides: "MoMo server Claude Code context"
      contains: "momo_create_payment"
    - path: "servers/mcp-zalopay-vn/CLAUDE.md"
      provides: "ZaloPay server Claude Code context"
      contains: "zalopay_create_order"
    - path: "servers/mcp-vnpay/CLAUDE.md"
      provides: "VNPAY server Claude Code context"
      contains: "vnpay_create_payment_url"
    - path: "servers/mcp-zalo-oa/CLAUDE.md"
      provides: "Zalo OA server Claude Code context"
      contains: "zalo_oa_send_message"
    - path: "servers/mcp-viettel-pay/CLAUDE.md"
      provides: "ViettelPay server Claude Code context"
      contains: "viettel_pay_create_payment"
    - path: "README.md"
      provides: "Root README positioning project as first VN MCP collection"
      contains: "Vietnamese MCP"
    - path: ".mcp.json"
      provides: "All 5 server entries for Claude Code"
      contains: "zalo-oa"
  key_links:
    - from: ".mcp.json"
      to: "servers/mcp-zalo-oa/build/index.js"
      via: "zalo-oa command entry"
      pattern: "mcp-zalo-oa/build/index.js"
    - from: ".mcp.json"
      to: "servers/mcp-viettel-pay/build/index.js"
      via: "viettel-pay command entry"
      pattern: "mcp-viettel-pay/build/index.js"
---

<objective>
Create CLAUDE.md and README.md for all 5 servers (retroactive for MoMo/ZaloPay/VNPAY, new for Zalo OA/ViettelPay), root README.md, update .mcp.json with all 5 entries, and verify full test suite passes.

Purpose: Documentation and integration completeness is the final gate before the hub is shippable. INFRA-07, INFRA-08, and INFRA-09 complete the v1 requirements.
Output: 10 documentation files (5 CLAUDE.md + 5 README.md), root README.md, updated .mcp.json, full test suite green.
</objective>

<execution_context>
@/Users/huynguyen/Desktop/vietnamese-mcp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huynguyen/Desktop/vietnamese-mcp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-zalo-oa-viettelpay-servers/04-CONTEXT.md
@.planning/phases/04-zalo-oa-viettelpay-servers/04-RESEARCH.md
@.planning/phases/04-zalo-oa-viettelpay-servers/04-01-SUMMARY.md
@.planning/phases/04-zalo-oa-viettelpay-servers/04-02-SUMMARY.md

<interfaces>
<!-- Executor needs tool names and env vars from all 5 servers to write accurate docs -->

MoMo tools (from servers/mcp-momo-vn/src/tools/):
- momo_create_payment: amount (int), orderInfo (string), redirectUrl?, ipnUrl?, requestType?, extraData?
- momo_query_status: orderId (string)
- momo_refund: transId (number), amount (int), description?
- momo_validate_ipn: ipnBody (string JSON)
Env: MOMO_SANDBOX, MOMO_PARTNER_CODE, MOMO_ACCESS_KEY, MOMO_SECRET_KEY

ZaloPay tools (from servers/mcp-zalopay-vn/src/tools/):
- zalopay_create_order: amount (int), description (string), bankCode?, returnUrl?, callbackUrl?
- zalopay_query_order: appTransId (string)
- zalopay_refund: zpTransId (string), amount (int), description?
- zalopay_validate_callback: data (string), mac (string), requestMac? (string)
Env: ZALOPAY_SANDBOX, ZALOPAY_APP_ID, ZALOPAY_KEY1, ZALOPAY_KEY2, ZALOPAY_ENDPOINT

VNPAY tools (from servers/mcp-vnpay/src/tools/):
- vnpay_create_payment_url: amount (int), orderInfo (string), returnUrl (string), ipAddr?, bankCode?, locale?
- vnpay_verify_return: returnUrl (string — full URL or query string)
- vnpay_query_transaction: txnRef (string), transDate (string YYYYMMDDHHMMSS), ipAddr?
Env: VNPAY_SANDBOX, VNPAY_TMN_CODE, VNPAY_HASH_SECRET, VNPAY_ENDPOINT

Zalo OA tools (from plan 04-01):
- zalo_oa_send_message: userId, type (text/image/file), text?, imageUrl?, fileUrl?
- zalo_oa_get_follower_profile: userId
- zalo_oa_list_followers: offset?, count?
- zalo_oa_refresh_token: (no params)
Env: ZALO_OA_SANDBOX, ZALO_OA_APP_ID, ZALO_OA_APP_SECRET, ZALO_OA_ACCESS_TOKEN, ZALO_OA_REFRESH_TOKEN

ViettelPay tools (from plan 04-02):
- viettel_pay_create_payment: amount, orderInfo, returnUrl?
- viettel_pay_query_status: transactionId
- viettel_pay_refund: transactionId, amount, description?
Env: VIETTELPAY_SANDBOX, VIETTEL_PAY_PARTNER_CODE, VIETTEL_PAY_SECRET_KEY, VIETTEL_PAY_ENDPOINT

Existing .mcp.json has 3 entries (momo-vn, zalopay-vn, vnpay). Add zalo-oa and viettel-pay.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CLAUDE.md and README.md for all 5 servers</name>
  <files>
    servers/mcp-momo-vn/CLAUDE.md,
    servers/mcp-momo-vn/README.md,
    servers/mcp-zalopay-vn/CLAUDE.md,
    servers/mcp-zalopay-vn/README.md,
    servers/mcp-vnpay/CLAUDE.md,
    servers/mcp-vnpay/README.md,
    servers/mcp-zalo-oa/CLAUDE.md,
    servers/mcp-zalo-oa/README.md,
    servers/mcp-viettel-pay/CLAUDE.md,
    servers/mcp-viettel-pay/README.md
  </files>
  <read_first>
    servers/mcp-momo-vn/src/tools/createPayment.ts,
    servers/mcp-momo-vn/src/tools/queryStatus.ts,
    servers/mcp-momo-vn/src/tools/refund.ts,
    servers/mcp-momo-vn/src/tools/validateIpn.ts,
    servers/mcp-zalopay-vn/src/tools/createOrder.ts,
    servers/mcp-zalopay-vn/src/tools/queryOrder.ts,
    servers/mcp-zalopay-vn/src/tools/refund.ts,
    servers/mcp-zalopay-vn/src/tools/validateCallback.ts,
    servers/mcp-vnpay/src/tools/createPaymentUrl.ts,
    servers/mcp-vnpay/src/tools/verifyReturn.ts,
    servers/mcp-vnpay/src/tools/queryTransaction.ts,
    servers/mcp-zalo-oa/src/tools/sendMessage.ts,
    servers/mcp-zalo-oa/src/tools/refreshToken.ts,
    servers/mcp-viettel-pay/src/tools/createPayment.ts,
    .mcp.json
  </read_first>
  <action>
Create 10 documentation files following the structure defined in 04-RESEARCH.md.

**CLAUDE.md per server** (5 files) — Each follows this exact structure:
```
# mcp-{server} -- Claude Code Context

## What This Server Does
[1-2 sentences about the server]

## Tools
| Tool | Description |
|------|-------------|
| {tool_name} | {one-liner from tool description} |

## Required Environment Variables
| Variable | Description | Mock Fallback |
|----------|-------------|---------------|
| {VAR} | {purpose} | {fallback value} |

## Enabling Mock Mode
Set {SERVICE}_SANDBOX=true in your .mcp.json env block.

## Common Workflows
[2-3 numbered workflow sequences showing tool chaining]
```

**Specific CLAUDE.md content per server:**

**mcp-momo-vn CLAUDE.md:**
- What: MoMo e-wallet payment gateway. Create payments, check status, process refunds, validate IPN callbacks.
- 4 tools: momo_create_payment, momo_query_status, momo_refund, momo_validate_ipn
- Env: MOMO_SANDBOX, MOMO_PARTNER_CODE (MOMOBKUN20180529), MOMO_ACCESS_KEY (klm05TvNBzhg7h7j), MOMO_SECRET_KEY (at67qH6mk8w5Y1nAyMoYKMWACiEi2bsa)
- Mock mode: MOMO_SANDBOX=true
- Workflows: (1) Create payment then query status, (2) Create payment then refund, (3) Validate incoming IPN

**mcp-zalopay-vn CLAUDE.md:**
- What: ZaloPay payment gateway. Create orders, check status, process refunds, validate callbacks.
- 4 tools: zalopay_create_order, zalopay_query_order, zalopay_refund, zalopay_validate_callback
- Env: ZALOPAY_SANDBOX, ZALOPAY_APP_ID (2553), ZALOPAY_KEY1 (PcY4iZIKFCIdgZvA6ueMcMHHUbRLYjPL), ZALOPAY_KEY2 (kLtgPl8HHhfvMuDHPwKfgfsY4Ydm9eIz), ZALOPAY_ENDPOINT (https://sb-openapi.zalopay.vn)
- Workflows: (1) Create order then query, (2) Validate callback MAC

**mcp-vnpay CLAUDE.md:**
- What: VNPAY payment gateway. Generate signed payment URLs, verify return URLs, query transactions.
- 3 tools: vnpay_create_payment_url, vnpay_verify_return, vnpay_query_transaction
- Env: VNPAY_SANDBOX, VNPAY_TMN_CODE (VNPAY_TMN_DEMO), VNPAY_HASH_SECRET (VNPAY_HASH_SECRET_DEMO), VNPAY_ENDPOINT
- Workflows: (1) Create payment URL then verify return, (2) Query transaction status

**mcp-zalo-oa CLAUDE.md:**
- What: Zalo Official Account messaging. Send messages, manage followers, refresh OAuth tokens.
- 4 tools: zalo_oa_send_message, zalo_oa_get_follower_profile, zalo_oa_list_followers, zalo_oa_refresh_token
- Env: ZALO_OA_SANDBOX, ZALO_OA_APP_ID (demo_app_id), ZALO_OA_APP_SECRET (demo_app_secret), ZALO_OA_ACCESS_TOKEN (demo_access_token), ZALO_OA_REFRESH_TOKEN (demo_refresh_token)
- Workflows: (1) List followers then send message, (2) Handle expired token by calling refresh_token first

**mcp-viettel-pay CLAUDE.md:**
- What: ViettelPay payment gateway (mock-only). See MOCK_DEVIATIONS.md for all assumptions.
- 3 tools: viettel_pay_create_payment, viettel_pay_query_status, viettel_pay_refund
- Env: VIETTELPAY_SANDBOX, VIETTEL_PAY_PARTNER_CODE (VTPAY_DEMO), VIETTEL_PAY_SECRET_KEY (demo_secret_key_vtpay), VIETTEL_PAY_ENDPOINT
- Add note: "This is a mock-only server. All API behavior is assumed. See MOCK_DEVIATIONS.md."
- Workflows: (1) Create payment then query, (2) Create payment then refund

**README.md per server** (5 files) — Each follows this structure:
```
# mcp-{server}

[One-line description]

## Quick Start

1. Install dependencies from monorepo root: `npm install`
2. Configure environment variables in `.mcp.json` (see below)
3. Add server entry to `.mcp.json`
4. Verify: `npm test --workspace=servers/mcp-{server}`

## .mcp.json Entry

{JSON block for this server's .mcp.json entry}

## Tools

### {tool_name}
{Description}

**Parameters:**
| Param | Type | Required | Description |
...

**Returns:** {description of response shape}

## Environment Variables

| Variable | Required | Description | Default |
...

## Mock Mode

Set `{SERVICE}_SANDBOX=true`. All tools return deterministic mock responses with `_mock: true`.
[For ViettelPay: add note about MOCK_DEVIATIONS.md]
```

Fill in each README with the actual tool parameters, return shapes, and env vars listed in the interfaces block above. Read each server's tool files first to get exact parameter names, types, and descriptions from the Zod schemas.
  </action>
  <verify>
    <automated>ls /Users/huynguyen/Desktop/vietnamese-mcp/servers/*/CLAUDE.md /Users/huynguyen/Desktop/vietnamese-mcp/servers/*/README.md 2>&1 | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - test -f servers/mcp-momo-vn/CLAUDE.md
    - test -f servers/mcp-momo-vn/README.md
    - test -f servers/mcp-zalopay-vn/CLAUDE.md
    - test -f servers/mcp-zalopay-vn/README.md
    - test -f servers/mcp-vnpay/CLAUDE.md
    - test -f servers/mcp-vnpay/README.md
    - test -f servers/mcp-zalo-oa/CLAUDE.md
    - test -f servers/mcp-zalo-oa/README.md
    - test -f servers/mcp-viettel-pay/CLAUDE.md
    - test -f servers/mcp-viettel-pay/README.md
    - grep -q 'momo_create_payment' servers/mcp-momo-vn/CLAUDE.md
    - grep -q 'zalopay_create_order' servers/mcp-zalopay-vn/CLAUDE.md
    - grep -q 'vnpay_create_payment_url' servers/mcp-vnpay/CLAUDE.md
    - grep -q 'zalo_oa_send_message' servers/mcp-zalo-oa/CLAUDE.md
    - grep -q 'viettel_pay_create_payment' servers/mcp-viettel-pay/CLAUDE.md
    - grep -q 'MOCK_DEVIATIONS' servers/mcp-viettel-pay/CLAUDE.md
  </acceptance_criteria>
  <done>
    All 5 servers have CLAUDE.md with tool catalog + env vars + workflows. All 5 servers have README.md with quick start + full tool reference. ViettelPay docs reference MOCK_DEVIATIONS.md.
  </done>
</task>

<task type="auto">
  <name>Task 2: Root README.md, update .mcp.json with all 5 servers, and verify full test suite</name>
  <files>
    README.md,
    .mcp.json
  </files>
  <read_first>
    .mcp.json,
    servers/mcp-momo-vn/CLAUDE.md,
    servers/mcp-zalo-oa/CLAUDE.md,
    servers/mcp-viettel-pay/CLAUDE.md
  </read_first>
  <action>
**Root README.md** — Written LAST (per Pitfall 6: root README after all servers complete):

```markdown
# Vietnamese MCP Server Collection

The first collection of Model Context Protocol (MCP) servers for Vietnamese fintech and messaging APIs. Plug into Claude Code and interact with Vietnam's major payment gateways and messaging platforms with zero integration boilerplate.

## Servers

| Server | Package | Description | Tools |
|--------|---------|-------------|-------|
| MoMo | `@vn-mcp/mcp-momo-vn` | MoMo e-wallet payments | 4 |
| ZaloPay | `@vn-mcp/mcp-zalopay-vn` | ZaloPay payment gateway | 4 |
| VNPAY | `@vn-mcp/mcp-vnpay` | VNPAY payment gateway | 3 |
| Zalo OA | `@vn-mcp/mcp-zalo-oa` | Zalo Official Account messaging | 4 |
| ViettelPay | `@vn-mcp/mcp-viettel-pay` | ViettelPay payments (mock-only) | 3 |

**Total: 18 tools** across 5 servers.

## Quick Start

1. Clone the repository:
   ```bash
   git clone <repo-url>
   cd vietnamese-mcp
   npm install
   ```

2. Copy the `.mcp.json` to your project or add server entries to your existing `.mcp.json`:
   ```json
   {see full .mcp.json content below}
   ```

3. Verify all servers work:
   ```bash
   npm test
   ```

All servers run in **mock mode** by default with `{SERVICE}_SANDBOX=true`. No API accounts needed.

## Architecture

This is a monorepo using npm workspaces:

- `packages/shared` -- Shared utilities (HMAC signing, mock engine, error formatting, test helpers)
- `servers/mcp-momo-vn` -- MoMo e-wallet ([README](servers/mcp-momo-vn/README.md))
- `servers/mcp-zalopay-vn` -- ZaloPay ([README](servers/mcp-zalopay-vn/README.md))
- `servers/mcp-vnpay` -- VNPAY ([README](servers/mcp-vnpay/README.md))
- `servers/mcp-zalo-oa` -- Zalo OA ([README](servers/mcp-zalo-oa/README.md))
- `servers/mcp-viettel-pay` -- ViettelPay ([README](servers/mcp-viettel-pay/README.md))

## Mock Mode

Every server runs in mock mode when `{SERVICE}_SANDBOX=true` is set. Mock responses are deterministic and include `_mock: true` in every response for easy identification.

No real API credentials are needed for development or testing.

## Development

```bash
npm install        # Install all workspaces
npm run build      # Build all packages
npm test           # Run all tests
```

## License

MIT
```

Adjust the exact formatting to be clean and professional. Ensure all server links are correct relative paths.

**.mcp.json** — Read existing file, add 2 new entries. Keep existing 3 entries exactly as-is. Add:

```json
"zalo-oa": {
  "command": "node",
  "args": ["./servers/mcp-zalo-oa/build/index.js"],
  "env": {
    "ZALO_OA_SANDBOX": "true",
    "ZALO_OA_APP_ID": "demo_app_id",
    "ZALO_OA_APP_SECRET": "demo_app_secret",
    "ZALO_OA_ACCESS_TOKEN": "demo_access_token",
    "ZALO_OA_REFRESH_TOKEN": "demo_refresh_token"
  }
},
"viettel-pay": {
  "command": "node",
  "args": ["./servers/mcp-viettel-pay/build/index.js"],
  "env": {
    "VIETTELPAY_SANDBOX": "true",
    "VIETTEL_PAY_PARTNER_CODE": "VTPAY_DEMO",
    "VIETTEL_PAY_SECRET_KEY": "demo_secret_key_vtpay",
    "VIETTEL_PAY_ENDPOINT": "https://sandbox.viettelpay.vn/vtpay-api"
  }
}
```

CRITICAL: The ViettelPay sandbox env var MUST be `VIETTELPAY_SANDBOX` (not `VIETTEL_PAY_SANDBOX`) because `isMockMode('viettelpay')` uppercases to `VIETTELPAY_SANDBOX`.

**Full test suite verification:** After writing all files, run `npm test` from repo root to verify all 5 servers pass.
  </action>
  <verify>
    <automated>cd /Users/huynguyen/Desktop/vietnamese-mcp && npm test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - test -f README.md
    - grep -q 'Vietnamese MCP' README.md
    - grep -q 'mcp-momo-vn' README.md
    - grep -q 'mcp-zalo-oa' README.md
    - grep -q 'mcp-viettel-pay' README.md
    - grep -q 'zalo-oa' .mcp.json
    - grep -q 'viettel-pay' .mcp.json
    - grep -q 'ZALO_OA_SANDBOX' .mcp.json
    - grep -q 'VIETTELPAY_SANDBOX' .mcp.json
    - grep -q 'mcp-zalo-oa/build/index.js' .mcp.json
    - grep -q 'mcp-viettel-pay/build/index.js' .mcp.json
    - npm test exits with code 0 (all 5 servers pass)
  </acceptance_criteria>
  <done>
    Root README.md positions project as first Vietnamese MCP collection. .mcp.json has all 5 server entries with correct env vars. `npm test` from repo root runs all 5 servers' tests and they all pass.
  </done>
</task>

</tasks>

<verification>
1. `ls servers/*/CLAUDE.md servers/*/README.md | wc -l` outputs 10
2. `cat .mcp.json | grep -c '"command"'` outputs 5
3. `npm test` from repo root — all tests across all 5 servers pass
4. `test -f README.md && grep -q 'Vietnamese MCP' README.md` — root README exists with correct positioning
</verification>

<success_criteria>
- All 5 servers have CLAUDE.md (INFRA-07) and README.md (INFRA-08)
- Root README.md presents the project as first Vietnamese MCP server collection
- .mcp.json has all 5 server entries with correct sandbox env vars
- `npm test` from repo root passes all integration tests across all 5 servers (INFRA-09)
- ViettelPay docs reference MOCK_DEVIATIONS.md
</success_criteria>

<output>
After completion, create `.planning/phases/04-zalo-oa-viettelpay-servers/04-03-SUMMARY.md`
</output>
