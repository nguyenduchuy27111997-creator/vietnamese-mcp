---
phase: 18-github-readme-seo
plan: 03
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - README.md
  - assets/demo.gif
autonomous: false
requirements:
  - GH-03

must_haves:
  truths:
    - "A GIF or video demo is embedded in the README showing the end-to-end flow"
    - "The demo shows signup, key creation, and a tool call returning a mock response"
  artifacts:
    - path: "assets/demo.gif"
      provides: "Animated GIF of the end-to-end flow"
    - path: "README.md"
      provides: "Root README with embedded GIF (uncommented)"
      contains: "demo.gif"
  key_links:
    - from: "README.md"
      to: "assets/demo.gif"
      via: "img tag"
      pattern: "assets/demo\\.gif"
---

<objective>
Add a GIF demo to the root README showing the end-to-end flow: signup, API key creation, and a tool call returning a mock MoMo payment response.

Purpose: A visual demo is the most persuasive element on a GitHub README — it proves the product works without requiring the viewer to install anything.
Output: assets/demo.gif file and updated README.md with embedded GIF
</objective>

<execution_context>
@/Users/huynguyen/Desktop/vietnamese-mcp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huynguyen/Desktop/vietnamese-mcp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-github-readme-seo/18-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create assets directory and prepare README GIF embed</name>
  <files>assets/.gitkeep, README.md</files>
  <read_first>
    - README.md (to find the GIF placeholder comment from plan 18-01)
  </read_first>
  <action>
1. Create `assets/` directory with a `.gitkeep` file.

2. In `README.md`, find the commented-out GIF placeholder block (from plan 18-01) and replace it with the active embed:

```html
<p align="center">
  <img src="./assets/demo.gif" alt="VN MCP Hub Demo — signup, create API key, make MoMo payment via Claude Code" width="720" />
</p>
```

Also add alt text that describes what the GIF shows for accessibility and SEO: "VN MCP Hub Demo — signup, create API key, make MoMo payment via Claude Code"

3. Add a note below the GIF:
```markdown
> **Demo:** Sign up, create an API key, and make a MoMo payment — all from Claude Code in under 60 seconds.
```
  </action>
  <verify>
    <automated>test -d /Users/huynguyen/Desktop/vietnamese-mcp/assets && grep -q "demo.gif" /Users/huynguyen/Desktop/vietnamese-mcp/README.md && ! grep -q "<!-- GIF demo will be added" /Users/huynguyen/Desktop/vietnamese-mcp/README.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - assets/ directory exists
    - README.md contains an uncommented img tag pointing to assets/demo.gif
    - The GIF placeholder comment is removed
    - Alt text is descriptive for accessibility
  </acceptance_criteria>
  <done>README.md has active GIF embed pointing to assets/demo.gif. The assets directory exists and is ready for the GIF file.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Record and add GIF demo</name>
  <files>assets/demo.gif</files>
  <action>
Human records a GIF demo of the end-to-end flow. Claude cannot record screen content.
The README embed is already in place from Task 1 — this task adds the actual GIF file.
  </action>
  <verify>
    <automated>test -f /Users/huynguyen/Desktop/vietnamese-mcp/assets/demo.gif && test $(stat -f%z /Users/huynguyen/Desktop/vietnamese-mcp/assets/demo.gif 2>/dev/null || echo 0) -gt 1000 && echo "PASS"</automated>
  </verify>
  <done>assets/demo.gif exists, is a valid GIF file over 1KB, and renders inline in the README on GitHub.</done>
  <what-built>The README is prepared with a GIF embed pointing to `assets/demo.gif`. The GIF file itself needs to be recorded by a human showing the end-to-end flow.</what-built>
  <how-to-verify>
**Record the GIF using a screen recording tool (e.g., Kap, LICEcap, or CleanShot X):**

1. Open the dashboard at https://vn-mcp-dashboard.pages.dev
2. Show the signup/login flow
3. Navigate to API Keys and create a new key
4. Switch to a terminal with Claude Code
5. Show a MoMo payment tool call (e.g., "Create a MoMo payment for 50,000 VND")
6. Show the mock response returning successfully

**Recording tips:**
- Resolution: 1280x720 or similar
- Keep it under 30 seconds if possible
- Crop to remove unnecessary browser chrome
- Target file size: under 5MB (GitHub renders GIFs inline up to 10MB)

**Save the GIF to:** `assets/demo.gif`

**After saving, verify:**
- Visit `README.md` in GitHub preview or `grip` — the GIF should render inline
- The GIF should be under 10MB for GitHub to display it

**Alternative:** If the full E2E flow is not available (Phase 13 validation not yet complete), record a simpler demo showing:
- The self-hosted npm path: `npm install -g @vn-mcp/mcp-momo-vn`
- A Claude Code tool call with mock response
- This is sufficient for launch and can be upgraded later
  </how-to-verify>
  <resume-signal>Type "approved" after adding the GIF to assets/demo.gif, or "skip" to leave the GIF placeholder for later and proceed with the phase.</resume-signal>
</task>

</tasks>

<verification>
- assets/demo.gif exists and is under 10MB
- README.md renders the GIF inline on GitHub
- The GIF shows a recognizable end-to-end flow (or at minimum a tool call demo)
</verification>

<success_criteria>
The root README.md displays an animated GIF demo that visually demonstrates the product value proposition — a developer sees the tool in action without installing anything.
</success_criteria>

<output>
After completion, create `.planning/phases/18-github-readme-seo/18-03-SUMMARY.md`
</output>
