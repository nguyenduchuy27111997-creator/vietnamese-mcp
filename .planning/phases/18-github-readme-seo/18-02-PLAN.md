---
phase: 18-github-readme-seo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - servers/mcp-momo-vn/README.md
  - servers/mcp-zalopay-vn/README.md
  - servers/mcp-vnpay/README.md
  - servers/mcp-zalo-oa/README.md
  - servers/mcp-viettel-pay/README.md
autonomous: true
requirements:
  - GH-04

must_haves:
  truths:
    - "Each per-server README includes an npm install command"
    - "Each per-server README includes a minimal .mcp.json snippet for the npm package (not monorepo path)"
    - "Each per-server README includes a tool call example prompt"
  artifacts:
    - path: "servers/mcp-momo-vn/README.md"
      provides: "MoMo server README with npm install"
      contains: "npm install"
    - path: "servers/mcp-zalopay-vn/README.md"
      provides: "ZaloPay server README with npm install"
      contains: "npm install"
    - path: "servers/mcp-vnpay/README.md"
      provides: "VNPAY server README with npm install"
      contains: "npm install"
    - path: "servers/mcp-zalo-oa/README.md"
      provides: "Zalo OA server README with npm install"
      contains: "npm install"
    - path: "servers/mcp-viettel-pay/README.md"
      provides: "ViettelPay server README with npm install"
      contains: "npm install"
  key_links:
    - from: "per-server READMEs"
      to: "npmjs.com package pages"
      via: "npm install commands and badge links"
      pattern: "npm install.*@vn-mcp"
---

<objective>
Update all 5 per-server README.md files with npm install instructions, .mcp.json snippets using the published npm binary (not monorepo paths), and example Claude Code prompts.

Purpose: A developer who finds a specific server package on npm or navigates to the server directory needs a self-contained quickstart without reading the root README.
Output: 5 updated per-server README.md files
</objective>

<execution_context>
@/Users/huynguyen/Desktop/vietnamese-mcp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huynguyen/Desktop/vietnamese-mcp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update payment server READMEs (MoMo, ZaloPay, VNPAY)</name>
  <files>servers/mcp-momo-vn/README.md, servers/mcp-zalopay-vn/README.md, servers/mcp-vnpay/README.md</files>
  <read_first>
    - servers/mcp-momo-vn/README.md
    - servers/mcp-zalopay-vn/README.md
    - servers/mcp-vnpay/README.md
    - servers/mcp-momo-vn/package.json (for exact package name and version)
    - servers/mcp-zalopay-vn/package.json
    - servers/mcp-vnpay/package.json
  </read_first>
  <action>
For each of the 3 payment server READMEs, rewrite with the following structure. KEEP the existing Tools section and Environment Variables section intact — only restructure the top sections and add new content.

**Structure for each README:**

1. **Badge + Title:**
```markdown
# @vn-mcp/{package-name}

[![npm version](https://img.shields.io/npm/v/@vn-mcp/{package-name})](https://www.npmjs.com/package/@vn-mcp/{package-name})
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](../../LICENSE)

{One-line description from current README}
```

2. **Install section (NEW — this is the key addition):**
```markdown
## Install

```bash
npm install -g @vn-mcp/{package-name}
```

3. **Configure section** — `.mcp.json` using the npm binary (NOT monorepo path):

For **mcp-momo-vn**:
```json
{
  "mcpServers": {
    "momo-vn": {
      "command": "mcp-momo-vn",
      "env": {
        "MOMO_SANDBOX": "true",
        "MOMO_PARTNER_CODE": "MOMOBKUN20180529",
        "MOMO_ACCESS_KEY": "klm05TvNBzhg7h7j",
        "MOMO_SECRET_KEY": "at67qH6mk8w5Y1nAyMoYKMWACiEi2bsa"
      }
    }
  }
}
```

For **mcp-zalopay-vn**:
```json
{
  "mcpServers": {
    "zalopay-vn": {
      "command": "mcp-zalopay-vn",
      "env": {
        "ZALOPAY_SANDBOX": "true",
        "ZALOPAY_APP_ID": "2553",
        "ZALOPAY_KEY1": "PcY4iZIKFCIdgZvA6ueMcMHHUbRLYjPL",
        "ZALOPAY_KEY2": "kLtgPl8HHhfvMuDHPwKfgfsY4Ydm9eIz",
        "ZALOPAY_ENDPOINT": "https://sb-openapi.zalopay.vn"
      }
    }
  }
}
```

For **mcp-vnpay**:
```json
{
  "mcpServers": {
    "vnpay": {
      "command": "mcp-vnpay",
      "env": {
        "VNPAY_SANDBOX": "true",
        "VNPAY_TMN_CODE": "VNPAY_TMN_DEMO",
        "VNPAY_HASH_SECRET": "VNPAY_HASH_SECRET_DEMO",
        "VNPAY_ENDPOINT": "https://sandbox.vnpayment.vn/paymentv2/vpcpay.html"
      }
    }
  }
}
```

4. **Example section (NEW):**
```markdown
## Example

After configuring `.mcp.json`, try in Claude Code:

> "{example prompt}"
```

Example prompts:
- MoMo: "Create a MoMo payment for 150,000 VND for order #1234"
- ZaloPay: "Create a ZaloPay order for 200,000 VND for coffee subscription"
- VNPAY: "Generate a VNPAY payment URL for 500,000 VND for electronics purchase"

5. **Keep existing sections:** Tools (with all parameter tables), Environment Variables, Mock Mode — preserve as-is.

6. **Add footer links:**
```markdown
## Links

- [Documentation](https://fpt-a833a5a1.mintlify.app/)
- [All Servers](../../README.md)
- [npm](https://www.npmjs.com/package/@vn-mcp/{package-name})
```

**CRITICAL:** The `.mcp.json` must use `"command": "{binary-name}"` (the npm bin name), NOT `"command": "node", "args": ["./servers/..."]`. This is the entire point of GH-04 — npm install instructions for standalone use.
  </action>
  <verify>
    <automated>for s in mcp-momo-vn mcp-zalopay-vn mcp-vnpay; do grep -q "npm install" /Users/huynguyen/Desktop/vietnamese-mcp/servers/$s/README.md && grep -q '"command": "mcp-' /Users/huynguyen/Desktop/vietnamese-mcp/servers/$s/README.md && grep -q "Example" /Users/huynguyen/Desktop/vietnamese-mcp/servers/$s/README.md || (echo "FAIL: $s" && exit 1); done && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Each README has npm install command
    - Each README has .mcp.json with npm binary command (not monorepo path)
    - Each README has an example Claude Code prompt
    - Existing tool documentation preserved
    - Footer links to docs, root README, and npm
  </acceptance_criteria>
  <done>MoMo, ZaloPay, and VNPAY server READMEs updated with npm install, binary-based .mcp.json, example prompts, and footer links.</done>
</task>

<task type="auto">
  <name>Task 2: Update messaging and ViettelPay server READMEs (Zalo OA, ViettelPay)</name>
  <files>servers/mcp-zalo-oa/README.md, servers/mcp-viettel-pay/README.md</files>
  <read_first>
    - servers/mcp-zalo-oa/README.md
    - servers/mcp-viettel-pay/README.md
    - servers/mcp-zalo-oa/package.json
    - servers/mcp-viettel-pay/package.json
  </read_first>
  <action>
Apply the same README structure as Task 1 to Zalo OA and ViettelPay servers.

**For mcp-zalo-oa:**

Badge: `@vn-mcp/mcp-zalo-oa`

Install: `npm install -g @vn-mcp/mcp-zalo-oa`

.mcp.json:
```json
{
  "mcpServers": {
    "zalo-oa": {
      "command": "mcp-zalo-oa",
      "env": {
        "ZALO_OA_SANDBOX": "true",
        "ZALO_OA_ACCESS_TOKEN": "mock_access_token",
        "ZALO_OA_REFRESH_TOKEN": "mock_refresh_token",
        "ZALO_OA_APP_ID": "mock_app_id",
        "ZALO_OA_APP_SECRET": "mock_app_secret"
      }
    }
  }
}
```
(Check the actual env vars from the existing README or package — adjust names to match what the server expects.)

Example prompt: "Send a Zalo OA message to user 12345 saying 'Your order has shipped!'"

**For mcp-viettel-pay:**

Badge: `@vn-mcp/mcp-viettel-pay`

Install: `npm install -g @vn-mcp/mcp-viettel-pay`

.mcp.json:
```json
{
  "mcpServers": {
    "viettel-pay": {
      "command": "mcp-viettel-pay",
      "env": {
        "VIETTELPAY_SANDBOX": "true",
        "VIETTELPAY_PARTNER_CODE": "VIETTEL_PARTNER_DEMO",
        "VIETTELPAY_SECRET_KEY": "VIETTEL_SECRET_DEMO"
      }
    }
  }
}
```
(Check the actual env vars from the existing README — adjust names to match.)

Example prompt: "Create a ViettelPay payment for 100,000 VND for mobile top-up"

Keep the ViettelPay mock-only note at the top. Keep existing tool docs and env vars intact.

Same footer links pattern as Task 1.

**CRITICAL:** Read each server's existing README first to get the correct env var names. Do not guess — use the exact variable names already documented.
  </action>
  <verify>
    <automated>for s in mcp-zalo-oa mcp-viettel-pay; do grep -q "npm install" /Users/huynguyen/Desktop/vietnamese-mcp/servers/$s/README.md && grep -q '"command": "mcp-' /Users/huynguyen/Desktop/vietnamese-mcp/servers/$s/README.md && grep -q "Example" /Users/huynguyen/Desktop/vietnamese-mcp/servers/$s/README.md || (echo "FAIL: $s" && exit 1); done && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Both READMEs have npm install command
    - Both READMEs have .mcp.json with npm binary command
    - Both READMEs have example Claude Code prompt
    - Zalo OA env vars match actual server config
    - ViettelPay keeps mock-only disclaimer
    - Existing tool documentation preserved
  </acceptance_criteria>
  <done>Zalo OA and ViettelPay server READMEs updated with npm install, binary-based .mcp.json, example prompts, and footer links. All 5 servers now have consistent README format.</done>
</task>

</tasks>

<verification>
- All 5 server READMEs contain `npm install -g @vn-mcp/{name}`
- All 5 server READMEs use binary command in .mcp.json (not monorepo node path)
- All 5 server READMEs have an Example section with a Claude Code prompt
- Existing tool documentation is preserved (not removed or truncated)
</verification>

<success_criteria>
A developer on any per-server npm page or GitHub directory can install the server globally, paste the .mcp.json config, and know exactly what to ask Claude Code — without needing to read the root README.
</success_criteria>

<output>
After completion, create `.planning/phases/18-github-readme-seo/18-02-SUMMARY.md`
</output>
