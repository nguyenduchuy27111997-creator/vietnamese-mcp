---
phase: 10-landing-page-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/docs/docs.json
  - apps/docs/index.mdx
  - apps/docs/quickstart.mdx
  - apps/docs/logo/logo.svg
autonomous: true
requirements: [SITE-01, SITE-02, SITE-04]

must_haves:
  truths:
    - "Landing page renders with hero, code snippet, pricing table, and signup CTA"
    - "Pricing table shows exactly 4 tiers with correct USD and VND values from provider.ts"
    - "Quickstart page has two tabs: Hosted (API Key) and Self-Hosted (npm)"
    - "Mintlify config (docs.json) defines valid navigation with all pages"
  artifacts:
    - path: "apps/docs/docs.json"
      provides: "Mintlify configuration — navigation, colors, branding"
      contains: '"$schema": "https://mintlify.com/docs.json"'
    - path: "apps/docs/index.mdx"
      provides: "Landing page with pricing and signup CTA"
      contains: 'mode: "custom"'
    - path: "apps/docs/quickstart.mdx"
      provides: "Quickstart with hosted/self-hosted tabs"
      contains: "<Tabs>"
  key_links:
    - from: "apps/docs/index.mdx"
      to: "dashboard signup"
      via: "CTA href"
      pattern: "dashboard"
    - from: "apps/docs/docs.json"
      to: "all MDX pages"
      via: "navigation.tabs.groups.pages"
      pattern: "quickstart"
---

<objective>
Scaffold the Mintlify docs site with configuration, landing page (pricing table + hero + CTA), and quickstart page (hosted/self-hosted tabs).

Purpose: Establishes the docs site foundation and the two highest-impact pages — landing page for conversion and quickstart for developer onboarding.
Output: apps/docs/ directory with docs.json, index.mdx, quickstart.mdx ready for local preview via `mint dev`.
</objective>

<execution_context>
@/Users/huynguyen/Desktop/vietnamese-mcp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huynguyen/Desktop/vietnamese-mcp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-landing-page-docs/10-CONTEXT.md
@.planning/phases/10-landing-page-docs/10-RESEARCH.md

<interfaces>
<!-- Authoritative pricing data from provider.ts -->
From apps/gateway/src/billing/provider.ts:
```typescript
export const STRIPE_TIERS: Record<BillingTier, { label: string; price: string }> = {
  starter: { label: 'Starter', price: '$19/mo' },
  pro: { label: 'Pro', price: '$49/mo' },
  business: { label: 'Business', price: '$149/mo' },
};

export const MOMO_TIERS: Record<BillingTier, { label: string; price: string; amountVnd: number }> = {
  starter: { label: 'Starter', price: '449,000 VND', amountVnd: 449000 },
  pro: { label: 'Pro', price: '1,190,000 VND', amountVnd: 1190000 },
  business: { label: 'Business', price: '3,590,000 VND', amountVnd: 3590000 },
};
```

From apps/gateway/src/metering/usageCounter.ts:
```typescript
export const TIER_LIMITS: Record<string, number> = {
  free: 1_000,
  starter: 10_000,
  pro: 100_000,
  business: Infinity,
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Mintlify config and landing page</name>
  <files>apps/docs/docs.json, apps/docs/index.mdx, apps/docs/logo/logo.svg</files>
  <read_first>
    - apps/gateway/src/billing/provider.ts
    - apps/gateway/src/metering/usageCounter.ts
    - .planning/phases/10-landing-page-docs/10-RESEARCH.md
  </read_first>
  <action>
1. Create `apps/docs/` directory structure: `apps/docs/logo/`, `apps/docs/servers/`

2. Create `apps/docs/logo/logo.svg` — a simple SVG text logo: "VN MCP" in a rounded rectangle, blue (#2563eb) background, white text. Keep it minimal (under 20 lines of SVG).

3. Create `apps/docs/docs.json` with this exact structure (docs.json NOT mint.json — renamed Feb 2025):
```json
{
  "$schema": "https://mintlify.com/docs.json",
  "name": "VN MCP",
  "theme": "mint",
  "logo": {
    "light": "/logo/logo.svg",
    "dark": "/logo/logo.svg"
  },
  "favicon": "/logo/logo.svg",
  "colors": {
    "primary": "#2563eb",
    "light": "#3b82f6",
    "dark": "#1d4ed8"
  },
  "navigation": {
    "tabs": [
      {
        "tab": "Documentation",
        "groups": [
          {
            "group": "Getting Started",
            "pages": ["quickstart"]
          },
          {
            "group": "Server Reference",
            "pages": [
              "servers/overview",
              "servers/momo",
              "servers/zalopay",
              "servers/vnpay",
              "servers/zalo-oa",
              "servers/viettel-pay"
            ]
          }
        ]
      }
    ],
    "global": {
      "anchors": [
        {
          "name": "Dashboard",
          "url": "https://dashboard.mcpvn.dev",
          "icon": "gauge"
        },
        {
          "name": "GitHub",
          "url": "https://github.com/vn-mcp/vietnamese-mcp",
          "icon": "github"
        }
      ]
    }
  },
  "topbar": {
    "style": "default"
  },
  "footerSocials": {
    "github": "https://github.com/vn-mcp/vietnamese-mcp"
  }
}
```

4. Create `apps/docs/index.mdx` — the landing page with `mode: "custom"` frontmatter. Content must include:
   - Title frontmatter: "Vietnamese Payment APIs for Claude Code"
   - Description: "5 MCP servers. 18 tools. Mock-first. Ready in 2 minutes."
   - Hero section: h1 "Vietnamese Payment APIs for Claude Code", subtitle about MCP protocol
   - Code snippet showing .mcp.json configuration with gateway URL and x-api-key header
   - Feature highlights section: "5 Servers" (MoMo, ZaloPay, VNPAY, Zalo OA, ViettelPay), "18 Tools" (payments, messaging, verification), "Mock-First" (sandbox mode, no real credentials needed), "MCP Protocol" (works with Claude Code out of the box)
   - Pricing table with EXACTLY these values (from provider.ts and usageCounter.ts):
     * Free: $0, 1,000 calls/month
     * Starter: $19/mo / 449,000 VND/mo, 10,000 calls/month
     * Pro: $49/mo / 1,190,000 VND/mo, 100,000 calls/month
     * Business: $149/mo / 3,590,000 VND/mo, Unlimited calls
   - Show USD as primary, mention VND as alternative (e.g., "or 449,000 VND/mo" next to each tier)
   - Signup CTA button: "Get started — it's free" linking to https://dashboard.mcpvn.dev
   - Server catalog section listing all 5 servers with their tool counts: MoMo (4), ZaloPay (4), VNPAY (3), Zalo OA (4), ViettelPay (3)

Use Mintlify MDX components where appropriate. The page uses `mode: "custom"` to remove sidebar/TOC.
  </action>
  <verify>
    <automated>ls apps/docs/docs.json apps/docs/index.mdx apps/docs/logo/logo.svg && grep -q '"\\$schema": "https://mintlify.com/docs.json"' apps/docs/docs.json && grep -q 'mode: "custom"' apps/docs/index.mdx && grep -q '\\$19/mo' apps/docs/index.mdx && grep -q '449,000 VND' apps/docs/index.mdx && grep -q '\\$49/mo' apps/docs/index.mdx && grep -q '\\$149/mo' apps/docs/index.mdx && grep -q '1,000' apps/docs/index.mdx && grep -q '10,000' apps/docs/index.mdx && grep -q '100,000' apps/docs/index.mdx && grep -q 'dashboard.mcpvn.dev' apps/docs/index.mdx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - apps/docs/docs.json exists with `"$schema": "https://mintlify.com/docs.json"` (NOT mint.json)
    - docs.json navigation references: quickstart, servers/overview, servers/momo, servers/zalopay, servers/vnpay, servers/zalo-oa, servers/viettel-pay
    - apps/docs/index.mdx has frontmatter `mode: "custom"`
    - index.mdx contains exact pricing: $19/mo, $49/mo, $149/mo, 449,000 VND, 1,190,000 VND, 3,590,000 VND
    - index.mdx contains call limits: 1,000, 10,000, 100,000, Unlimited
    - index.mdx has CTA linking to dashboard.mcpvn.dev
    - apps/docs/logo/logo.svg exists
  </acceptance_criteria>
  <done>docs.json config valid with full navigation; landing page has hero, pricing table with correct values from provider.ts, feature highlights, server catalog, and signup CTA</done>
</task>

<task type="auto">
  <name>Task 2: Create quickstart page with hosted/self-hosted tabs</name>
  <files>apps/docs/quickstart.mdx</files>
  <read_first>
    - apps/docs/docs.json
    - servers/mcp-momo-vn/CLAUDE.md
    - servers/mcp-momo-vn/README.md
    - .planning/phases/10-landing-page-docs/10-RESEARCH.md
  </read_first>
  <action>
Create `apps/docs/quickstart.mdx` — a single quickstart page using Mintlify `<Tabs>` and `<Tab>` components (locked decision: single page with tabs, no page splitting).

Frontmatter:
```
---
title: "Quickstart"
description: "Go from zero to first tool call in under 5 minutes."
---
```

Content structure using `<Tabs>` component:

**Tab 1: "Hosted (API Key)"**
- Step 1: Sign up — link to dashboard.mcpvn.dev, mention free tier = 1,000 calls/month, no credit card
- Step 2: Generate API key — go to dashboard, click "Create Key", copy the sk_test_* key
- Step 3: Configure .mcp.json — show exact JSON config:
```json
{
  "mcpServers": {
    "vn-payments": {
      "url": "https://gateway.mcpvn.dev/mcp",
      "headers": {
        "x-api-key": "YOUR_API_KEY"
      }
    }
  }
}
```
- Step 4: Make first tool call — "Ask Claude Code: Create a MoMo payment for 50,000 VND"
- Expected result: Show example response with orderId, payUrl, resultCode: 0, _mock: true

**Tab 2: "Self-Hosted (npm)"**
- Step 1: Install — `npm install -g @vn-mcp/mcp-momo-vn` (use MoMo as the example server)
- Step 2: Configure .mcp.json — show exact JSON config with npx command and env vars:
```json
{
  "mcpServers": {
    "momo-vn": {
      "command": "npx",
      "args": ["-y", "@vn-mcp/mcp-momo-vn"],
      "env": {
        "MOMO_SANDBOX": "true",
        "MOMO_PARTNER_CODE": "MOMOBKUN20180529",
        "MOMO_ACCESS_KEY": "klm05TvNBzhg7h7j",
        "MOMO_SECRET_KEY": "at67qH6mk8w5Y1nAyMoYKMWACiEi2bsa"
      }
    }
  }
}
```
- Step 3: Make first tool call — same as hosted tab
- Step 4: Add more servers — table of all 5 npm package names (@vn-mcp/mcp-momo-vn, @vn-mcp/mcp-zalopay-vn, @vn-mcp/mcp-vnpay, @vn-mcp/mcp-zalo-oa, @vn-mcp/mcp-viettel-pay)

After the tabs, add a "What's next?" section linking to server reference pages.
  </action>
  <verify>
    <automated>ls apps/docs/quickstart.mdx && grep -q '<Tabs>' apps/docs/quickstart.mdx && grep -q 'Hosted (API Key)' apps/docs/quickstart.mdx && grep -q 'Self-Hosted (npm)' apps/docs/quickstart.mdx && grep -q '@vn-mcp/mcp-momo-vn' apps/docs/quickstart.mdx && grep -q 'gateway.mcpvn.dev' apps/docs/quickstart.mdx && grep -q 'dashboard.mcpvn.dev' apps/docs/quickstart.mdx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - apps/docs/quickstart.mdx exists with title "Quickstart"
    - Contains `<Tabs>` and `<Tab>` components (Mintlify native)
    - Tab 1 titled "Hosted (API Key)" with gateway URL and x-api-key header
    - Tab 2 titled "Self-Hosted (npm)" with npx command and env vars
    - References dashboard.mcpvn.dev for signup
    - Contains all 5 npm package names
    - Contains MoMo sandbox credentials (MOMOBKUN20180529, klm05TvNBzhg7h7j, at67qH6mk8w5Y1nAyMoYKMWACiEi2bsa)
  </acceptance_criteria>
  <done>Quickstart page has two working tabs covering both hosted and self-hosted paths with correct URLs, package names, and example configs</done>
</task>

</tasks>

<verification>
- All files exist: `ls apps/docs/docs.json apps/docs/index.mdx apps/docs/quickstart.mdx apps/docs/logo/logo.svg`
- docs.json uses new schema (not mint.json): `grep '$schema' apps/docs/docs.json`
- Pricing matches provider.ts exactly: `grep -c '19/mo\|49/mo\|149/mo\|449,000\|1,190,000\|3,590,000' apps/docs/index.mdx` should return >= 6
- Landing page is custom layout: `grep 'mode: "custom"' apps/docs/index.mdx`
- Quickstart has tabs: `grep '<Tabs>' apps/docs/quickstart.mdx`
</verification>

<success_criteria>
- docs.json valid Mintlify config with full navigation referencing all planned pages
- Landing page shows correct pricing from provider.ts, feature highlights, server catalog, signup CTA
- Quickstart covers both hosted and self-hosted paths in tabbed layout
- All files under apps/docs/ ready for Plan 02 to add server reference pages
</success_criteria>

<output>
After completion, create `.planning/phases/10-landing-page-docs/10-01-SUMMARY.md`
</output>
