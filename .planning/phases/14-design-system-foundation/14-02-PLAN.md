---
phase: 14-design-system-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - apps/dashboard/src/components/ui/button.tsx
  - apps/dashboard/src/components/ui/card.tsx
  - apps/dashboard/src/components/ui/badge.tsx
  - apps/dashboard/src/components/ui/dialog.tsx
  - apps/dashboard/src/components/ui/input.tsx
  - apps/dashboard/src/components/ui/table.tsx
  - apps/dashboard/src/components/ui/tabs.tsx
  - apps/dashboard/src/components/ui/dropdown-menu.tsx
  - apps/dashboard/src/components/ui/sheet.tsx
  - apps/dashboard/src/components/ui/switch.tsx
  - apps/dashboard/src/components/ui/progress.tsx
  - apps/dashboard/src/components/ui/alert.tsx
  - apps/dashboard/src/components/ui/tooltip.tsx
  - apps/dashboard/src/components/ui/avatar.tsx
  - apps/dashboard/src/components/theme-toggle.tsx
  - apps/dashboard/src/components/theme-provider.tsx
autonomous: true
requirements:
  - DS-01
  - DS-02

must_haves:
  truths:
    - "shadcn/ui Button, Card, Badge, Dialog render correctly with project color tokens"
    - "A theme toggle switches between dark and light mode"
    - "Theme choice persists across page reloads via localStorage"
    - "All 14 shadcn/ui components needed for phases 15-17 are installed and importable"
  artifacts:
    - path: "apps/dashboard/src/components/ui/button.tsx"
      provides: "shadcn/ui Button component"
      exports: ["Button"]
    - path: "apps/dashboard/src/components/ui/card.tsx"
      provides: "shadcn/ui Card component"
      exports: ["Card", "CardHeader", "CardTitle", "CardDescription", "CardContent", "CardFooter"]
    - path: "apps/dashboard/src/components/theme-toggle.tsx"
      provides: "Dark/light mode toggle button"
      exports: ["ThemeToggle"]
    - path: "apps/dashboard/src/components/theme-provider.tsx"
      provides: "Theme context provider"
      exports: ["ThemeProvider"]
  key_links:
    - from: "apps/dashboard/src/components/theme-toggle.tsx"
      to: "localStorage"
      via: "reads/writes theme preference and toggles dark class on html"
      pattern: "localStorage.*theme"
    - from: "apps/dashboard/src/components/ui/button.tsx"
      to: "apps/dashboard/src/globals.css"
      via: "uses CSS custom properties through Tailwind classes"
      pattern: "bg-primary"
    - from: "apps/dashboard/src/App.tsx"
      to: "apps/dashboard/src/components/theme-provider.tsx"
      via: "wraps app in ThemeProvider"
      pattern: "ThemeProvider"
---

<objective>
Install all shadcn/ui components needed for the dashboard redesign and create a dark/light theme toggle with persistence.

Purpose: With the toolchain from Plan 01, install the actual UI component library and theme switching mechanism so phases 15-17 can import and use components immediately.
Output: 14 shadcn/ui components installed, ThemeProvider context, ThemeToggle button, App.tsx wrapped in provider.
</objective>

<execution_context>
@/Users/huynguyen/Desktop/vietnamese-mcp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huynguyen/Desktop/vietnamese-mcp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-design-system-foundation/14-CONTEXT.md
@.planning/phases/14-design-system-foundation/14-01-SUMMARY.md

@apps/dashboard/package.json
@apps/dashboard/components.json
@apps/dashboard/tailwind.config.ts
@apps/dashboard/src/globals.css
@apps/dashboard/src/lib/utils.ts
@apps/dashboard/src/App.tsx
@apps/dashboard/src/main.tsx
</context>

<interfaces>
<!-- From Plan 01 outputs that this plan depends on -->

From apps/dashboard/src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```

From apps/dashboard/components.json:
```json
{
  "style": "new-york",
  "aliases": {
    "ui": "@/components/ui",
    "utils": "@/lib/utils"
  }
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install all 14 shadcn/ui components via CLI</name>
  <files>
    apps/dashboard/src/components/ui/button.tsx,
    apps/dashboard/src/components/ui/card.tsx,
    apps/dashboard/src/components/ui/badge.tsx,
    apps/dashboard/src/components/ui/dialog.tsx,
    apps/dashboard/src/components/ui/input.tsx,
    apps/dashboard/src/components/ui/table.tsx,
    apps/dashboard/src/components/ui/tabs.tsx,
    apps/dashboard/src/components/ui/dropdown-menu.tsx,
    apps/dashboard/src/components/ui/sheet.tsx,
    apps/dashboard/src/components/ui/switch.tsx,
    apps/dashboard/src/components/ui/progress.tsx,
    apps/dashboard/src/components/ui/alert.tsx,
    apps/dashboard/src/components/ui/tooltip.tsx,
    apps/dashboard/src/components/ui/avatar.tsx
  </files>
  <read_first>
    apps/dashboard/components.json,
    apps/dashboard/package.json,
    apps/dashboard/src/lib/utils.ts
  </read_first>
  <action>
1. From the `apps/dashboard` directory, install all 14 shadcn/ui components using the CLI. Run each component install individually (shadcn CLI does not support batch install reliably):

   ```bash
   cd apps/dashboard
   npx shadcn@latest add button --yes --overwrite
   npx shadcn@latest add card --yes --overwrite
   npx shadcn@latest add badge --yes --overwrite
   npx shadcn@latest add dialog --yes --overwrite
   npx shadcn@latest add input --yes --overwrite
   npx shadcn@latest add table --yes --overwrite
   npx shadcn@latest add tabs --yes --overwrite
   npx shadcn@latest add dropdown-menu --yes --overwrite
   npx shadcn@latest add sheet --yes --overwrite
   npx shadcn@latest add switch --yes --overwrite
   npx shadcn@latest add progress --yes --overwrite
   npx shadcn@latest add alert --yes --overwrite
   npx shadcn@latest add tooltip --yes --overwrite
   npx shadcn@latest add avatar --yes --overwrite
   ```

   NOTE: The `--yes` flag auto-confirms prompts. If the CLI adds additional peer dependencies (like @radix-ui packages), let it install them automatically.

2. After installation, verify each component file exists at `src/components/ui/{name}.tsx` and imports from `@/lib/utils`.

3. Run `npm install` from the monorepo root to ensure all new @radix-ui dependencies are properly resolved in the lockfile.
  </action>
  <verify>
    <automated>cd /Users/huynguyen/Desktop/vietnamese-mcp/apps/dashboard && for comp in button card badge dialog input table tabs dropdown-menu sheet switch progress alert tooltip avatar; do test -f "src/components/ui/$comp.tsx" || { echo "MISSING: $comp.tsx"; exit 1; }; done && echo "All 14 components installed"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/dashboard/src/components/ui/button.tsx` succeeds
    - `test -f apps/dashboard/src/components/ui/card.tsx` succeeds
    - `test -f apps/dashboard/src/components/ui/badge.tsx` succeeds
    - `test -f apps/dashboard/src/components/ui/dialog.tsx` succeeds
    - `test -f apps/dashboard/src/components/ui/input.tsx` succeeds
    - `test -f apps/dashboard/src/components/ui/table.tsx` succeeds
    - `test -f apps/dashboard/src/components/ui/tabs.tsx` succeeds
    - `test -f apps/dashboard/src/components/ui/dropdown-menu.tsx` succeeds
    - `test -f apps/dashboard/src/components/ui/sheet.tsx` succeeds
    - `test -f apps/dashboard/src/components/ui/switch.tsx` succeeds
    - `test -f apps/dashboard/src/components/ui/progress.tsx` succeeds
    - `test -f apps/dashboard/src/components/ui/alert.tsx` succeeds
    - `test -f apps/dashboard/src/components/ui/tooltip.tsx` succeeds
    - `test -f apps/dashboard/src/components/ui/avatar.tsx` succeeds
    - `grep -rq "@/lib/utils" apps/dashboard/src/components/ui/` succeeds (components use cn utility)
  </acceptance_criteria>
  <done>All 14 shadcn/ui components (button, card, badge, dialog, input, table, tabs, dropdown-menu, sheet, switch, progress, alert, tooltip, avatar) are installed at src/components/ui/ with correct imports.</done>
</task>

<task type="auto">
  <name>Task 2: Create ThemeProvider, ThemeToggle, wire into App.tsx, and verify build</name>
  <files>
    apps/dashboard/src/components/theme-provider.tsx,
    apps/dashboard/src/components/theme-toggle.tsx,
    apps/dashboard/src/App.tsx
  </files>
  <read_first>
    apps/dashboard/src/App.tsx,
    apps/dashboard/src/components/ui/button.tsx,
    apps/dashboard/index.html
  </read_first>
  <action>
1. Create `apps/dashboard/src/components/theme-provider.tsx`:
   ```tsx
   import { createContext, useContext, useEffect, useState } from 'react';

   type Theme = 'dark' | 'light' | 'system';

   interface ThemeProviderState {
     theme: Theme;
     setTheme: (theme: Theme) => void;
   }

   const ThemeProviderContext = createContext<ThemeProviderState>({
     theme: 'dark',
     setTheme: () => null,
   });

   export function ThemeProvider({
     children,
     defaultTheme = 'dark',
     storageKey = 'theme',
   }: {
     children: React.ReactNode;
     defaultTheme?: Theme;
     storageKey?: string;
   }) {
     const [theme, setTheme] = useState<Theme>(
       () => (localStorage.getItem(storageKey) as Theme) || defaultTheme
     );

     useEffect(() => {
       const root = window.document.documentElement;
       root.classList.remove('light', 'dark');

       if (theme === 'system') {
         const systemTheme = window.matchMedia('(prefers-color-scheme: dark)').matches
           ? 'dark'
           : 'light';
         root.classList.add(systemTheme);
       } else {
         root.classList.add(theme);
       }
     }, [theme]);

     const value = {
       theme,
       setTheme: (newTheme: Theme) => {
         localStorage.setItem(storageKey, newTheme);
         setTheme(newTheme);
       },
     };

     return (
       <ThemeProviderContext.Provider value={value}>
         {children}
       </ThemeProviderContext.Provider>
     );
   }

   export function useTheme() {
     const context = useContext(ThemeProviderContext);
     if (context === undefined)
       throw new Error('useTheme must be used within a ThemeProvider');
     return context;
   }
   ```

2. Create `apps/dashboard/src/components/theme-toggle.tsx`:
   ```tsx
   import { Moon, Sun } from 'lucide-react';
   import { Button } from '@/components/ui/button';
   import { useTheme } from '@/components/theme-provider';

   export function ThemeToggle() {
     const { theme, setTheme } = useTheme();

     const isDark = theme === 'dark' || (theme === 'system' && window.matchMedia('(prefers-color-scheme: dark)').matches);

     return (
       <Button
         variant="ghost"
         size="icon"
         onClick={() => setTheme(isDark ? 'light' : 'dark')}
         aria-label="Toggle theme"
       >
         {isDark ? (
           <Sun className="h-4 w-4" />
         ) : (
           <Moon className="h-4 w-4" />
         )}
       </Button>
     );
   }
   ```

3. Update `apps/dashboard/src/App.tsx` to wrap the entire app in ThemeProvider and render the ThemeToggle in the top-right corner as a floating button (temporary placement — Phase 15 will move it to the sidebar):

   ```tsx
   import { useEffect, useState } from 'react';
   import type { Session } from '@supabase/supabase-js';
   import { supabase } from './supabase.js';
   import { AuthPage } from './pages/AuthPage.js';
   import { DashboardPage } from './pages/DashboardPage.js';
   import { ThemeProvider } from '@/components/theme-provider';
   import { ThemeToggle } from '@/components/theme-toggle';

   export default function App() {
     const [session, setSession] = useState<Session | null>(null);
     const [loading, setLoading] = useState(true);

     useEffect(() => {
       supabase.auth.getSession().then(({ data }) => {
         setSession(data.session);
         setLoading(false);
       });

       const { data: { subscription } } = supabase.auth.onAuthStateChange((_event, sess) => {
         setSession(sess);
       });

       return () => subscription.unsubscribe();
     }, []);

     if (loading) return null;

     return (
       <ThemeProvider defaultTheme="dark">
         <div className="fixed top-4 right-4 z-50">
           <ThemeToggle />
         </div>
         {session
           ? <DashboardPage userEmail={session.user.email ?? ''} />
           : <AuthPage />}
       </ThemeProvider>
     );
   }
   ```

4. Run the build to verify everything compiles:
   ```bash
   cd apps/dashboard && npx vite build
   ```
  </action>
  <verify>
    <automated>cd /Users/huynguyen/Desktop/vietnamese-mcp && npm run --workspace=apps/dashboard build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "ThemeProvider" apps/dashboard/src/components/theme-provider.tsx` succeeds
    - `grep -q "useTheme" apps/dashboard/src/components/theme-provider.tsx` succeeds
    - `grep -q "localStorage" apps/dashboard/src/components/theme-provider.tsx` succeeds
    - `grep -q "ThemeToggle" apps/dashboard/src/components/theme-toggle.tsx` succeeds
    - `grep -q "Moon" apps/dashboard/src/components/theme-toggle.tsx` succeeds (lucide icon)
    - `grep -q "Sun" apps/dashboard/src/components/theme-toggle.tsx` succeeds (lucide icon)
    - `grep -q "ThemeProvider" apps/dashboard/src/App.tsx` succeeds (provider wraps app)
    - `grep -q "ThemeToggle" apps/dashboard/src/App.tsx` succeeds (toggle rendered)
    - `npm run --workspace=apps/dashboard build` succeeds with exit code 0
  </acceptance_criteria>
  <done>ThemeProvider manages dark/light/system theme with localStorage persistence. ThemeToggle renders Sun/Moon icon and toggles theme. App.tsx wraps in ThemeProvider with dark default. Build succeeds with all components and theme system wired.</done>
</task>

</tasks>

<verification>
1. `npm run --workspace=apps/dashboard build` succeeds with zero errors
2. All 14 shadcn/ui component files exist in `apps/dashboard/src/components/ui/`
3. ThemeProvider and ThemeToggle exist and export correctly
4. App.tsx imports and renders ThemeProvider + ThemeToggle
5. Theme toggle persists choice to localStorage under key "theme"
</verification>

<success_criteria>
- All 14 shadcn/ui components installed and importable (button, card, badge, dialog, input, table, tabs, dropdown-menu, sheet, switch, progress, alert, tooltip, avatar)
- ThemeProvider context manages dark/light/system themes
- ThemeToggle button switches between dark and light modes
- Theme preference persists to localStorage across page reloads
- Dashboard build succeeds with all new components
- Components use project color tokens via CSS custom properties (not hardcoded colors)
</success_criteria>

<output>
After completion, create `.planning/phases/14-design-system-foundation/14-02-SUMMARY.md`
</output>
