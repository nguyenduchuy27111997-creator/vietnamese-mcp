---
phase: 19-example-apps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/payment-checkout/package.json
  - examples/payment-checkout/index.html
  - examples/payment-checkout/src/main.tsx
  - examples/payment-checkout/src/App.tsx
  - examples/payment-checkout/src/App.css
  - examples/payment-checkout/vite.config.ts
  - examples/payment-checkout/tsconfig.json
  - examples/payment-checkout/.env.example
  - examples/payment-checkout/.mcp.json
  - examples/payment-checkout/README.md
autonomous: true
requirements:
  - EX-01
  - EX-03
must_haves:
  truths:
    - "Developer can clone repo, cd into examples/payment-checkout, run npm install && npm run dev, and see a payment UI in the browser"
    - "Clicking 'Pay with MoMo' calls the hosted gateway and displays a mock payment URL response"
    - "README contains prerequisites, 3-step setup, .mcp.json snippet, and a screenshot/GIF placeholder"
  artifacts:
    - path: "examples/payment-checkout/package.json"
      provides: "React + Vite project with minimal deps"
      contains: "\"dev\": \"vite\""
    - path: "examples/payment-checkout/src/App.tsx"
      provides: "Payment checkout UI with MoMo and VNPAY buttons"
      min_lines: 60
    - path: "examples/payment-checkout/README.md"
      provides: "Setup instructions and demo screenshot"
      min_lines: 40
    - path: "examples/payment-checkout/.mcp.json"
      provides: "Claude Code MCP config for hosted gateway"
      contains: "vn-mcp-gateway"
  key_links:
    - from: "examples/payment-checkout/src/App.tsx"
      to: "https://vn-mcp-gateway.nguyenduchuy27111997.workers.dev"
      via: "fetch call to /mcp/momo endpoint"
      pattern: "fetch.*vn-mcp-gateway"
---

<objective>
Create a minimal React payment checkout example app that demonstrates creating MoMo and VNPAY payments through the hosted VN MCP Gateway.

Purpose: Gives developers a clone-and-run starter template showing how to use VN MCP Hub for payment integration.
Output: Complete `examples/payment-checkout/` directory with working React app, README, and .mcp.json config.
</objective>

<execution_context>
@/Users/huynguyen/Desktop/vietnamese-mcp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huynguyen/Desktop/vietnamese-mcp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

Key facts:
- Gateway URL: https://vn-mcp-gateway.nguyenduchuy27111997.workers.dev
- MoMo endpoint: /mcp/momo (POST with JSON-RPC body)
- VNPAY endpoint: /mcp/vnpay
- Auth: Bearer token in Authorization header
- npm packages: @vn-mcp/mcp-momo-vn@1.0.2, @vn-mcp/mcp-vnpay@1.0.2
- All servers return mock responses in sandbox mode (no real credentials needed)
- Root README quickstart shows .mcp.json format for both hosted and self-hosted
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold React payment checkout app</name>
  <files>
    examples/payment-checkout/package.json,
    examples/payment-checkout/index.html,
    examples/payment-checkout/src/main.tsx,
    examples/payment-checkout/src/App.tsx,
    examples/payment-checkout/src/App.css,
    examples/payment-checkout/vite.config.ts,
    examples/payment-checkout/tsconfig.json,
    examples/payment-checkout/.env.example,
    examples/payment-checkout/.mcp.json
  </files>
  <read_first>
    - README.md (lines 50-92, hosted gateway .mcp.json format)
    - servers/mcp-momo-vn/package.json (version, bin name)
    - servers/mcp-vnpay/package.json (version, bin name)
  </read_first>
  <action>
Create a minimal React + Vite + TypeScript app in examples/payment-checkout/.

**package.json** — standalone (NOT part of monorepo workspaces):
```json
{
  "name": "vn-mcp-payment-checkout-example",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
```

**vite.config.ts** — basic React plugin config, nothing fancy.

**tsconfig.json** — standard Vite React TS config (target ES2022, jsx react-jsx, strict true).

**index.html** — standard Vite entry with div#root.

**src/main.tsx** — renders App into root.

**.env.example**:
```
VITE_GATEWAY_URL=https://vn-mcp-gateway.nguyenduchuy27111997.workers.dev
VITE_API_KEY=your_api_key_here
```

**.mcp.json** — shows how Claude Code users would configure the hosted gateway:
```json
{
  "mcpServers": {
    "momo-hosted": {
      "type": "streamable-http",
      "url": "https://vn-mcp-gateway.nguyenduchuy27111997.workers.dev/mcp/momo",
      "headers": {
        "Authorization": "Bearer YOUR_API_KEY"
      }
    },
    "vnpay-hosted": {
      "type": "streamable-http",
      "url": "https://vn-mcp-gateway.nguyenduchuy27111997.workers.dev/mcp/vnpay",
      "headers": {
        "Authorization": "Bearer YOUR_API_KEY"
      }
    }
  }
}
```

**src/App.tsx** — minimal payment checkout UI:
- Title: "VN MCP Payment Checkout"
- Subtitle: "Example app using VN MCP Hub hosted gateway"
- Amount input (default: 50000 VND) and order description input
- Two buttons: "Pay with MoMo" and "Pay with VNPAY"
- On click, send a JSON-RPC request to the gateway:
  - MoMo: POST to `${VITE_GATEWAY_URL}/mcp/momo` with body:
    ```json
    {
      "jsonrpc": "2.0",
      "id": 1,
      "method": "tools/call",
      "params": {
        "name": "momo_create_payment",
        "arguments": { "amount": 50000, "orderInfo": "Example payment" }
      }
    }
    ```
  - VNPAY: same pattern to `/mcp/vnpay` with `vnpay_create_payment_url` tool, arguments `{ "amount": 50000, "orderDescription": "Example payment", "returnUrl": "http://localhost:5173" }`
- Headers: `{ "Content-Type": "application/json", "Authorization": "Bearer ${VITE_API_KEY}" }`
- Display response JSON in a pre block below buttons
- Show loading spinner while request in flight
- Show error message if fetch fails (red text)
- Keep styling MINIMAL — use App.css with basic flexbox centering, system fonts, light gray background, card-like white container. No CSS framework needed.

**src/App.css** — simple styles:
- Centered card layout, max-width 480px, padding 2rem
- Buttons: MoMo button pink (#d82d8b), VNPAY button blue (#005baa)
- Input fields: full width, padding, border, border-radius
- Response block: monospace, background #f5f5f5, overflow auto
- Keep under 80 lines

IMPORTANT: This is a STANDALONE app. Do NOT add it to root workspaces array. Do NOT import from @vn-mcp packages. It uses the hosted gateway HTTP API directly.
  </action>
  <verify>
    <automated>cd /Users/huynguyen/Desktop/vietnamese-mcp/examples/payment-checkout && cat package.json | grep -q '"dev": "vite"' && cat src/App.tsx | grep -q "momo_create_payment" && cat .mcp.json | grep -q "vn-mcp-gateway" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - package.json has react, vite, typescript deps with dev script
    - App.tsx has MoMo and VNPAY payment buttons with fetch calls to gateway
    - .env.example has VITE_GATEWAY_URL and VITE_API_KEY
    - .mcp.json has momo-hosted and vnpay-hosted server configs
    - App is standalone (not in root workspaces)
  </acceptance_criteria>
  <done>All files exist, package.json parseable, App.tsx references both momo_create_payment and vnpay_create_payment_url tools, .mcp.json has correct gateway URLs</done>
</task>

<task type="auto">
  <name>Task 2: Write payment checkout README with setup and screenshot placeholder</name>
  <files>examples/payment-checkout/README.md</files>
  <read_first>
    - examples/payment-checkout/package.json (just created)
    - examples/payment-checkout/.mcp.json (just created)
    - examples/payment-checkout/.env.example (just created)
  </read_first>
  <action>
Create examples/payment-checkout/README.md with the following exact structure:

```markdown
# Payment Checkout Example

A minimal React app that creates MoMo and VNPAY payments through the [VN MCP Hub](https://github.com/nguyenduchuy27111997-creator/vietnamese-mcp) hosted gateway.

![Payment Checkout Demo](./screenshot.png)

> Screenshot shows the checkout UI with MoMo and VNPAY payment buttons and a mock API response.

## Prerequisites

- Node.js >= 20
- A VN MCP Hub API key ([sign up here](https://vn-mcp-dashboard.pages.dev))

## Setup (3 steps)

### 1. Install dependencies

​```bash
cd examples/payment-checkout
npm install
​```

### 2. Configure your API key

​```bash
cp .env.example .env
​```

Edit `.env` and replace `your_api_key_here` with your actual API key:

​```
VITE_GATEWAY_URL=https://vn-mcp-gateway.nguyenduchuy27111997.workers.dev
VITE_API_KEY=sk_test_your_actual_key
​```

### 3. Run the app

​```bash
npm run dev
​```

Open http://localhost:5173 — click "Pay with MoMo" or "Pay with VNPAY" to see the mock payment response.

## How it works

This app sends JSON-RPC requests to the VN MCP Gateway, which routes them to the appropriate MCP server:

- **Pay with MoMo** calls `momo_create_payment` → returns a mock payment URL
- **Pay with VNPAY** calls `vnpay_create_payment_url` → returns a mock payment URL

All responses are mock/sandbox data — no real money is charged.

## Claude Code Integration

This example also includes a `.mcp.json` file for Claude Code users. Add it to your project and Claude can create payments directly:

​```json
{
  "mcpServers": {
    "momo-hosted": {
      "type": "streamable-http",
      "url": "https://vn-mcp-gateway.nguyenduchuy27111997.workers.dev/mcp/momo",
      "headers": {
        "Authorization": "Bearer YOUR_API_KEY"
      }
    }
  }
}
​```

Then ask Claude: "Create a MoMo payment for 100,000 VND for order #12345"

## Tech Stack

- React 19 + TypeScript
- Vite 6
- VN MCP Hub hosted gateway (no SDK — plain fetch)

## Related

- [VN MCP Hub README](../../README.md)
- [MoMo Server Docs](../../servers/mcp-momo-vn/README.md)
- [VNPAY Server Docs](../../servers/mcp-vnpay/README.md)
```

Also create a placeholder screenshot.png: create a simple text file at examples/payment-checkout/screenshot.png with a note that this should be replaced with an actual screenshot. Actually, do NOT create a fake binary — just note in the README that the screenshot will be added. Use the img tag with alt text so it gracefully degrades:

Replace the `![Payment Checkout Demo](./screenshot.png)` with:
```html
<p align="center">
  <img src="./screenshot.png" alt="Payment Checkout Example — React app with MoMo and VNPAY buttons showing mock payment response" width="600" />
</p>
```

This way the alt text shows if the image is missing, and it renders nicely once a screenshot is added.
  </action>
  <verify>
    <automated>cd /Users/huynguyen/Desktop/vietnamese-mcp/examples/payment-checkout && cat README.md | grep -q "Prerequisites" && cat README.md | grep -q "npm install" && cat README.md | grep -q ".mcp.json" && cat README.md | grep -q "screenshot" && wc -l README.md | awk '{if ($1 >= 40) print "PASS"; else print "FAIL: only " $1 " lines"}'</automated>
  </verify>
  <acceptance_criteria>
    - README has Prerequisites section listing Node >= 20 and API key
    - README has 3-step setup (install, configure, run)
    - README has .mcp.json snippet for Claude Code
    - README has screenshot/GIF embed (placeholder OK, must have img tag)
    - README is at least 40 lines
  </acceptance_criteria>
  <done>README.md exists with prerequisites, 3-step setup, .mcp.json config, screenshot placeholder, and at least 40 lines of content</done>
</task>

</tasks>

<verification>
- `ls examples/payment-checkout/` shows package.json, src/, README.md, .mcp.json, .env.example
- `cat examples/payment-checkout/src/App.tsx` contains fetch calls to gateway with momo_create_payment and vnpay_create_payment_url
- `cat examples/payment-checkout/README.md` has prerequisites, setup steps, .mcp.json snippet
- App is NOT in root package.json workspaces array
</verification>

<success_criteria>
A developer can clone the repo, cd into examples/payment-checkout, run `npm install && npm run dev`, and see a working payment checkout UI that calls the hosted VN MCP Gateway. The README provides clear 3-step setup instructions.
</success_criteria>

<output>
After completion, create `.planning/phases/19-example-apps/19-01-SUMMARY.md`
</output>
