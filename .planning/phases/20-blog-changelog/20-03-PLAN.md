---
phase: 20-blog-changelog
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/docs/changelog.mdx
autonomous: true
requirements: [BLOG-03]

must_haves:
  truths:
    - "Changelog page exists at /changelog on the Mintlify site"
    - "Entries cover v1.0, v1.1, v1.2, and v2.0 with dates and feature summaries"
    - "A developer can see the project history at a glance"
  artifacts:
    - path: "apps/docs/changelog.mdx"
      provides: "Changelog page with version history"
      min_lines: 60
      contains: "v1.0"
  key_links:
    - from: "apps/docs/docs.json"
      to: "apps/docs/changelog.mdx"
      via: "Getting Started navigation group"
      pattern: "changelog"
---

<objective>
Create a changelog page for the Mintlify docs site covering v1.0 through v2.0 releases.

Purpose: The changelog gives developers confidence the project is actively maintained and lets them see the full feature history at a glance. Required for Product Hunt credibility.
Output: One MDX changelog page at apps/docs/changelog.mdx.
</objective>

<execution_context>
@/Users/huynguyen/Desktop/vietnamese-mcp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huynguyen/Desktop/vietnamese-mcp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the changelog page</name>
  <files>apps/docs/changelog.mdx</files>
  <read_first>.planning/ROADMAP.md</read_first>
  <action>
Create `apps/docs/changelog.mdx` with the full version history. Source dates and feature summaries from ROADMAP.md phase completion dates.

**Frontmatter:**
```yaml
---
title: "Changelog"
description: "Release history for VN MCP Hub — from v1.0 MCP servers to v2.0 modern dashboard."
sidebarTitle: "Changelog"
icon: "clock-rotate-left"
---
```

**Content structure — write as a reverse-chronological changelog (newest first):**

```
## v2.0 — Modern Dashboard (March 26, 2026)

A complete dashboard redesign with dark mode, new design system, and polished UI.

**New:**
- Design system foundation with Tailwind CSS + shadcn/ui, dark mode default
- Collapsible sidebar navigation with mobile responsive layout
- Overview page with stat cards (API keys, usage, tier)
- API Keys page with search, filter, create modal, copy animation, revoke confirmation
- Usage page with 30-day area chart and per-server breakdown
- Billing page with Stripe (USD) and MoMo (VND) payment options
- Settings page with password change and account deletion
- Quickstart onboarding wizard (3-step: create key, configure, test)

---

## v1.2 — Production Deployment (March 25, 2026)

Production-ready deployment with E2E validation.

**New:**
- Dashboard deployed to Cloudflare Pages
- Docs deployed to Mintlify cloud
- Auth test stubs implemented with real assertions
- Tinybird tool name extraction fixed
- Full E2E user journey validated (signup through billing)

---

## v1.1 — Platform Launch (March 25, 2026)

From standalone servers to a full developer platform.

**New:**
- Cloudflare Workers gateway — all 5 servers via Streamable HTTP at a single endpoint
- Supabase Auth with email/password signup and API key management
- KV-cached API key authentication with 60-second TTL
- Tier-based access control (Free: 2 servers, Starter/Pro/Business: all 5)
- Tinybird metering — fire-and-forget event logging with monthly usage limits
- Stripe Checkout (USD) and MoMo IPN (VND) billing with webhook idempotency
- npm packages published under @vn-mcp scope for self-hosted use
- Mintlify docs site with pricing, quickstart, and per-server tool reference
- Developer dashboard with key management, usage display, and billing

---

## v1.0 — MCP Servers (March 21, 2026)

The initial release — 5 MCP servers for Vietnamese fintech and messaging.

**New:**
- **MoMo** server — create payments, check status, refund, validate IPN (4 tools)
- **ZaloPay** server — create orders, query, refund, validate callbacks (4 tools)
- **VNPAY** server — generate signed payment URLs, verify returns, query transactions (3 tools)
- **Zalo OA** server — send messages, get profiles, list followers, refresh tokens (4 tools)
- **ViettelPay** server — create payments, query status, refund (3 tools, mock-only)
- Monorepo with shared utilities (HMAC signing, error formatting)
- Sandbox/mock mode for all servers — works offline with no real credentials
- 18 tools total across all 5 servers
```

At the bottom, add:

```
---

<Note>
Dates reflect when each milestone was feature-complete in the repository. Public availability depends on npm publish and deployment status.
</Note>
```

Total length: 70-100 lines. Keep entries factual and scannable — bullet points, not paragraphs. Use `---` horizontal rules between versions.
  </action>
  <verify>
    <automated>test -f apps/docs/changelog.mdx && grep -c "^## v" apps/docs/changelog.mdx | awk '{if($1>=4) print "PASS: "$1" version entries"; else {print "FAIL: only "$1" entries"; exit 1}}' && wc -l apps/docs/changelog.mdx | awk '{if($1>=60) print "PASS: "$1" lines"; else {print "FAIL: "$1" lines"; exit 1}}'</automated>
  </verify>
  <acceptance_criteria>changelog.mdx exists with 4+ version entries (v1.0, v1.1, v1.2, v2.0), each with date and bullet-point feature list, 60+ lines total</acceptance_criteria>
  <done>Changelog page has v1.0 through v2.0 entries with dates, feature summaries, and is referenced in docs.json navigation</done>
</task>

</tasks>

<verification>
- apps/docs/changelog.mdx exists with 60+ lines
- Contains 4 version headers (v1.0, v1.1, v1.2, v2.0)
- Each version has a date, summary line, and feature bullet list
- Page slug "changelog" matches the reference added to docs.json in plan 20-01
</verification>

<success_criteria>
Changelog page written with v1.0 through v2.0 entries, reverse-chronological, with dates and feature summaries sourced from ROADMAP.md milestones.
</success_criteria>

<output>
After completion, create `.planning/phases/20-blog-changelog/20-03-SUMMARY.md`
</output>
