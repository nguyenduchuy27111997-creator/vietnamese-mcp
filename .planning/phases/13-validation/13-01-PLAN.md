---
phase: 13-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [VAL-01, VAL-02]

must_haves:
  truths:
    - "A new user can sign up on the deployed dashboard"
    - "A signed-in user can create an API key and copy it"
    - "A tool call with that API key through the production gateway returns a valid MCP response"
    - "The usage bar in the dashboard increments after the tool call"
    - "A free-tier user can initiate Stripe Checkout and see the upgrade flow"
  artifacts: []
  key_links:
    - from: "dashboard signup form"
      to: "Supabase Auth"
      via: "supabase.auth.signUp"
      pattern: "POST.*supabase.*auth"
    - from: "dashboard create key"
      to: "gateway /keys"
      via: "fetch POST /keys"
      pattern: "POST.*/keys"
    - from: "curl tools/call"
      to: "gateway /mcp/momo"
      via: "HTTP POST with Bearer token"
      pattern: "POST.*/mcp/momo"
    - from: "dashboard usage bar"
      to: "gateway /usage"
      via: "fetch GET /usage"
      pattern: "GET.*/usage"
    - from: "dashboard upgrade button"
      to: "gateway /billing/create-checkout"
      via: "fetch POST /billing/create-checkout"
      pattern: "POST.*/billing/create-checkout"
---

<objective>
Validate the full hosted E2E user journey in production: signup, API key creation, MCP tool call, usage tracking, and billing upgrade flow.

Purpose: Confirm VAL-01 (signup through usage) and VAL-02 (billing upgrade) work end-to-end on deployed infrastructure.
Output: Human-verified pass/fail for each flow segment.
</objective>

<execution_context>
@/Users/huynguyen/Desktop/vietnamese-mcp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huynguyen/Desktop/vietnamese-mcp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

Production URLs:
- Dashboard: https://vn-mcp-dashboard.pages.dev
- Gateway: https://vn-mcp-gateway.nguyenduchuy27111997.workers.dev
- Docs: https://fpt-a833a5a1.mintlify.app/
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: VAL-01 — Signup, API Key, Tool Call, Usage</name>
  <read_first>
    - apps/gateway/src/index.ts (route structure)
    - apps/dashboard/src/pages/DashboardPage.tsx (UI flow)
  </read_first>
  <what-built>Full hosted platform: dashboard on CF Pages, gateway on CF Workers, Supabase auth, Tinybird metering</what-built>
  <how-to-verify>
Follow these steps exactly and report pass/fail for each:

**Step 1: Signup**
1. Open https://vn-mcp-dashboard.pages.dev in a browser
2. EXPECTED: Login/signup form loads (not a 404 or blank page)
3. Enter a NEW email and password, click Sign Up
4. Check your email for the Supabase confirmation link — click it
5. EXPECTED: You are redirected back to the dashboard and see the API Keys page
6. PASS/FAIL: ___

**Step 2: Create API Key**
1. On the dashboard, click "+ Create key"
2. EXPECTED: A green banner appears showing your new API key (starts with `sk_test_`)
3. Copy the full API key and save it — it will not be shown again
4. EXPECTED: The key appears in the table below with its prefix and creation date
5. PASS/FAIL: ___

**Step 3: MCP Tool Call**
1. Open a terminal and run the following curl command, replacing YOUR_KEY with the key you copied:

```bash
curl -X POST https://vn-mcp-gateway.nguyenduchuy27111997.workers.dev/mcp/momo \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_KEY" \
  -d '{
    "jsonrpc": "2.0",
    "id": 1,
    "method": "tools/call",
    "params": {
      "name": "momo_create_payment",
      "arguments": {
        "amount": 50000,
        "orderInfo": "Test payment",
        "orderId": "TEST-001",
        "redirectUrl": "https://example.com/return",
        "ipnUrl": "https://example.com/ipn",
        "requestId": "req-001"
      }
    }
  }'
```

2. EXPECTED: JSON response with `"jsonrpc": "2.0"` and a result containing tool output (mock payment URL). NOT a 401, 403, or 500 error.
3. PASS/FAIL: ___

**Step 4: Usage Tracking**
1. Return to the dashboard at https://vn-mcp-dashboard.pages.dev
2. Refresh the page
3. EXPECTED: The usage bar shows at least 1 call used (e.g., "1 / 1,000 calls this month")
4. PASS/FAIL: ___

**Step 5: Health Check (bonus)**
1. Run: `curl https://vn-mcp-gateway.nguyenduchuy27111997.workers.dev/health`
2. EXPECTED: 200 response (confirms gateway is live)
3. PASS/FAIL: ___
  </how-to-verify>
  <action>Human follows the step-by-step instructions above and reports pass/fail for each step.</action>
  <acceptance_criteria>
    - Steps 1-4 all pass
    - Signup completes without errors
    - API key is created and visible in the dashboard
    - curl tool call returns valid MCP JSON-RPC response
    - Usage bar shows incremented count after tool call
  </acceptance_criteria>
  <resume-signal>Report pass/fail for each of the 5 steps. If any step fails, describe the exact error or unexpected behavior.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: VAL-02 — Stripe Checkout Billing Upgrade</name>
  <read_first>
    - apps/dashboard/src/hooks/useBilling.ts (billing hook)
    - apps/gateway/src/routes/billing.ts (billing routes)
  </read_first>
  <what-built>Stripe Checkout integration: dashboard upgrade CTA, gateway checkout session creation, webhook tier upgrade</what-built>
  <how-to-verify>
Follow these steps exactly and report pass/fail for each:

**Prerequisite:** You must be logged in to the dashboard from Task 1 with a free-tier account.

**Step 1: Verify Free Tier**
1. On the dashboard, confirm your tier shows "Free" next to your email
2. EXPECTED: Yellow upgrade banner visible saying "Upgrade to unlock more API calls and servers"
3. EXPECTED: "Pay with Card ($19/mo)" button is visible
4. PASS/FAIL: ___

**Step 2: Initiate Stripe Checkout**
1. Click "Pay with Card ($19/mo)" button
2. EXPECTED: Browser redirects to a Stripe Checkout page showing the Starter plan at $19/month
3. NOTE: If you see a CORS error or network error in the console, report the exact error message
4. PASS/FAIL: ___

**Step 3: Complete Payment (Stripe Test Mode)**
1. On the Stripe Checkout page, use these test card details:
   - Card number: 4242 4242 4242 4242
   - Expiry: any future date (e.g., 12/30)
   - CVC: any 3 digits (e.g., 123)
   - Name: Test User
   - Country: United States (or any)
2. Click "Subscribe" or "Pay"
3. EXPECTED: Stripe processes the payment and redirects you back to the dashboard
4. PASS/FAIL: ___

**Step 4: Verify Tier Upgrade**
1. After redirect, check the dashboard
2. EXPECTED: Tier now shows "Starter" (not "Free")
3. EXPECTED: The yellow upgrade banner is replaced with a blue "Starter plan active" banner with "Manage subscription" button
4. NOTE: If tier does not update immediately, wait 10-15 seconds and refresh — the webhook may take a moment
5. PASS/FAIL: ___

**Step 5: Verify Increased Limits**
1. Run the same curl tool call from Task 1 Step 3
2. EXPECTED: Response still works (tool call succeeds)
3. The usage limit should now be 10,000/month (Starter tier) instead of 1,000 (Free tier)
4. Check the usage bar on the dashboard — it should show the higher limit
5. PASS/FAIL: ___
  </how-to-verify>
  <action>Human follows the step-by-step instructions above and reports pass/fail for each step.</action>
  <acceptance_criteria>
    - Stripe Checkout page loads from the dashboard CTA
    - Test payment completes successfully
    - Tier upgrades from Free to Starter after webhook delivery
    - Usage limit reflects the new Starter tier (10,000/month)
  </acceptance_criteria>
  <resume-signal>Report pass/fail for each of the 5 steps. If any step fails, describe the exact error or unexpected behavior.</resume-signal>
</task>

</tasks>

<verification>
All steps in both tasks pass. The full hosted journey from signup to billing upgrade works in production.
</verification>

<success_criteria>
- VAL-01: New user signed up, created key, made tool call, saw usage — all in production
- VAL-02: Free-tier user completed Stripe Checkout, tier upgraded to Starter within one webhook delivery
</success_criteria>

<output>
After completion, create `.planning/phases/13-validation/13-01-SUMMARY.md`
</output>
