---
phase: 04-zalo-oa-viettelpay-servers
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - servers/mcp-viettel-pay/package.json
  - servers/mcp-viettel-pay/tsconfig.json
  - servers/mcp-viettel-pay/vitest.config.ts
  - servers/mcp-viettel-pay/MOCK_DEVIATIONS.md
  - servers/mcp-viettel-pay/src/index.ts
  - servers/mcp-viettel-pay/src/credentials.ts
  - servers/mcp-viettel-pay/src/signatures.ts
  - servers/mcp-viettel-pay/src/client.ts
  - servers/mcp-viettel-pay/src/tools/createPayment.ts
  - servers/mcp-viettel-pay/src/tools/queryStatus.ts
  - servers/mcp-viettel-pay/src/tools/refund.ts
  - servers/mcp-viettel-pay/src/tools/index.ts
  - servers/mcp-viettel-pay/src/mock/createPayment.json
  - servers/mcp-viettel-pay/src/mock/queryStatus.json
  - servers/mcp-viettel-pay/src/mock/refund.json
  - servers/mcp-viettel-pay/src/mock/errorInsufficientBalance.json
  - servers/mcp-viettel-pay/src/__tests__/integration.test.ts
autonomous: true
requirements:
  - VTPAY-01
  - VTPAY-02
  - VTPAY-03
  - VTPAY-04

must_haves:
  truths:
    - "Claude Code can call viettel_pay_create_payment and receive a mock payment response with _mock:true"
    - "Claude Code can call viettel_pay_query_status with a transactionId and receive a mock status with _mock:true"
    - "Claude Code can call viettel_pay_refund and receive a mock refund confirmation with _mock:true"
    - "viettel_pay_create_payment with amount=99999999 returns isError:true (insufficient balance)"
    - "MOCK_DEVIATIONS.md documents every assumption with Field/Assumed Value/Source/Confidence/Note columns"
    - "All ViettelPay tools pass vitest integration tests when VIETTELPAY_SANDBOX=true"
  artifacts:
    - path: "servers/mcp-viettel-pay/MOCK_DEVIATIONS.md"
      provides: "Per-field assumption documentation for mock-only server"
      contains: "Auth scheme"
    - path: "servers/mcp-viettel-pay/src/client.ts"
      provides: "Mock/real client switcher for ViettelPay"
      contains: "isMockMode('viettelpay')"
    - path: "servers/mcp-viettel-pay/src/signatures.ts"
      provides: "HMAC-SHA256 signature builder (assumed, documented in MOCK_DEVIATIONS)"
      contains: "signHmacSha256"
    - path: "servers/mcp-viettel-pay/src/__tests__/integration.test.ts"
      provides: "Integration tests for all 3 ViettelPay tools"
      contains: "VIETTELPAY_SANDBOX"
  key_links:
    - from: "servers/mcp-viettel-pay/src/tools/createPayment.ts"
      to: "servers/mcp-viettel-pay/src/client.ts"
      via: "import { viettelPayClient }"
      pattern: "viettelPayClient\\.createPayment"
    - from: "servers/mcp-viettel-pay/src/client.ts"
      to: "packages/shared/src/mock-engine/isMockMode.ts"
      via: "import { isMockMode }"
      pattern: "isMockMode\\('viettelpay'\\)"
    - from: "servers/mcp-viettel-pay/src/signatures.ts"
      to: "packages/shared/src/http-client/hmac.ts"
      via: "import { signHmacSha256 }"
      pattern: "signHmacSha256"
---

<objective>
Build the complete mcp-viettel-pay MCP server with 3 payment tools, MOCK_DEVIATIONS.md, mock fixtures, and integration tests.

Purpose: ViettelPay is a mock-only server demonstrating the pattern for low-confidence API integrations. MOCK_DEVIATIONS.md is the key artifact — it documents every assumption explicitly.
Output: Working mcp-viettel-pay package with 3 tools passing all integration tests in mock mode, plus comprehensive MOCK_DEVIATIONS.md.
</objective>

<execution_context>
@/Users/huynguyen/Desktop/vietnamese-mcp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huynguyen/Desktop/vietnamese-mcp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-zalo-oa-viettelpay-servers/04-CONTEXT.md
@.planning/phases/04-zalo-oa-viettelpay-servers/04-RESEARCH.md

<interfaces>
<!-- Shared utilities the executor MUST import -->

From packages/shared (already built, import directly):
```typescript
export function isMockMode(service: string): boolean; // isMockMode('viettelpay') checks VIETTELPAY_SANDBOX env
export function loadFixture<T>(path: string): T & { _mock: true };
export class McpApiError extends Error { constructor(code: string, message: string, provider: string, suggestion: string); }
export function formatToolError(err: unknown): { isError: true; content: Array<{ type: string; text: string }> };
export function signHmacSha256(data: string, key: string): string; // From packages/shared/src/http-client/hmac.ts
export function createTestClient(server: McpServer): Promise<Client>;
export function callTool(client: Client, name: string, args: Record<string, unknown>): Promise<{ isError?: boolean; content: Array<{ type: string; text: string }> }>;
```

From packages/shared/src/errors/error-codes.ts (already has viettelpay entries):
```typescript
viettelpay: {
  '00': 'Success',
  '06': 'Insufficient balance',
},
```

Mock client pattern (from servers/mcp-momo-vn/src/client.ts):
```typescript
// Error trigger: amount === 99999999 throws McpApiError
// Mock mode: isMockMode('momo') -> loadFixture -> return curated response
// Real mode: throw Error('Real API not implemented')
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MOCK_DEVIATIONS.md, scaffold package, credentials, signatures, client, and mock fixtures</name>
  <files>
    servers/mcp-viettel-pay/MOCK_DEVIATIONS.md,
    servers/mcp-viettel-pay/package.json,
    servers/mcp-viettel-pay/tsconfig.json,
    servers/mcp-viettel-pay/vitest.config.ts,
    servers/mcp-viettel-pay/src/credentials.ts,
    servers/mcp-viettel-pay/src/signatures.ts,
    servers/mcp-viettel-pay/src/client.ts,
    servers/mcp-viettel-pay/src/mock/createPayment.json,
    servers/mcp-viettel-pay/src/mock/queryStatus.json,
    servers/mcp-viettel-pay/src/mock/refund.json,
    servers/mcp-viettel-pay/src/mock/errorInsufficientBalance.json
  </files>
  <read_first>
    servers/mcp-momo-vn/package.json,
    servers/mcp-momo-vn/tsconfig.json,
    servers/mcp-momo-vn/vitest.config.ts,
    servers/mcp-momo-vn/src/credentials.ts,
    servers/mcp-momo-vn/src/signatures.ts,
    servers/mcp-momo-vn/src/client.ts,
    servers/mcp-momo-vn/src/mock/createPayment.json,
    packages/shared/src/errors/error-codes.ts
  </read_first>
  <action>
**CRITICAL: Write MOCK_DEVIATIONS.md FIRST** (per Pitfall 4 in research). Every mock response field must have a row here before being used in client.ts.

**MOCK_DEVIATIONS.md:**
```markdown
# ViettelPay Mock Deviations

This server is **mock-only**. ViettelPay has no confirmed public REST API documentation.
The real enterprise API uses SOAP + RSA (per `giautm/viettelpay` Go client), not the
REST + HMAC pattern used here. All API behavior is assumed for internal consistency
with the other MCP servers in this monorepo.

## Assumption Table

| Field | Assumed Value | Source | Confidence | Note |
|-------|---------------|--------|------------|------|
| Auth scheme | HMAC-SHA256 | Inferred from VN payment API industry pattern (MoMo, ZaloPay) | LOW | Real ViettelPay enterprise API uses SOAP+RSA per giautm/viettelpay Go client |
| Endpoint base URL | `https://sandbox.viettelpay.vn/vtpay-api` | Third-party reference | LOW | Not confirmed from official docs; never called in mock mode |
| Request format | JSON REST | Inferred from MoMo/ZaloPay pattern | LOW | Real API uses Gzip-compressed JSON over SOAP |
| Response format | `{ code, message, data: { ... } }` | Inferred from VN payment API convention | LOW | Not confirmed |
| Success code | `'00'` | VN_ERROR_CODES stub in error-codes.ts | LOW | Matches VN convention (VNPAY also uses '00') |
| Insufficient balance code | `'06'` | VN_ERROR_CODES stub in error-codes.ts | LOW | Assumed |
| transactionId format | `VTP_` + 12-char hex | Inferred from MoMo MOMO_ prefix pattern | LOW | Not confirmed |
| paymentUrl format | `https://sandbox.viettelpay.vn/pay/{transactionId}` | Assumed placeholder | LOW | Real flow likely differs (SOAP-based) |
| Partner code env var | `VIETTEL_PAY_PARTNER_CODE` | Inferred from MoMo MOMO_PARTNER_CODE pattern | LOW | Real API uses username/password/serviceCode |
| Secret key env var | `VIETTEL_PAY_SECRET_KEY` | Inferred from MoMo MOMO_SECRET_KEY pattern | LOW | Real API uses RSA key pairs |
| Endpoint env var | `VIETTEL_PAY_ENDPOINT` | Inferred from ZaloPay ZALOPAY_ENDPOINT pattern | LOW | Placeholder only |
| Refund response | Same shape as createPayment | Inferred from MoMo refund pattern | LOW | Not confirmed |
| Query response | Status string + original transaction fields | Inferred from MoMo queryStatus pattern | LOW | Not confirmed |

## Real API Contrast

The real ViettelPay enterprise API (from `giautm/viettelpay` Go package) uses:
- **SOAP protocol** (not REST JSON)
- **RSA asymmetric crypto** (partner private key + Viettel public key)
- **Username/password/serviceCode** authentication envelope
- **Gzip-compressed JSON** payloads

This mock uses REST + HMAC-SHA256 purely for internal consistency. If real API access
is ever obtained, the server will need a complete rewrite of signatures.ts and client.ts.
```

**package.json** — name: `@vn-mcp/mcp-viettel-pay`, version: `0.0.1`, type: `module`, bin: `./build/index.js`, scripts: `build` (tsdown src/index.ts --format esm --dts), `dev` (tsx src/index.ts), `test` (VIETTELPAY_SANDBOX=true vitest run). Same deps as Zalo OA: `@modelcontextprotocol/sdk: ^1.27.1`, `zod: ^3.25.76`, `@vn-mcp/shared: *`. Engines: node >=20.

**tsconfig.json** — identical structure to mcp-momo-vn.

**vitest.config.ts** — identical to mcp-momo-vn.

**credentials.ts** — export type `ViettelPayCredentials = { partnerCode: string; secretKey: string; endpoint: string }`. Export function `getViettelPayCredentials()` reading `VIETTEL_PAY_PARTNER_CODE` (fallback `VTPAY_DEMO`), `VIETTEL_PAY_SECRET_KEY` (fallback `demo_secret_key_vtpay`), `VIETTEL_PAY_ENDPOINT` (fallback `https://sandbox.viettelpay.vn/vtpay-api`).

**signatures.ts** — import `signHmacSha256` from `@vn-mcp/shared`. Export `buildViettelPaySignature(data: string, secretKey: string): string` that calls `signHmacSha256(data, secretKey)`. Add JSDoc comment: `/** Assumed HMAC-SHA256 — see MOCK_DEVIATIONS.md. Real ViettelPay uses RSA. */`

**client.ts** — export `viettelPayClient` with 3 methods. Use same pattern as MoMo:

1. `createPayment(args: { amount: number; orderInfo: string; returnUrl?: string })`:
   - If `args.amount === 99999999`, throw `new McpApiError('06', 'Insufficient balance', 'viettelpay', 'Try a smaller amount')`.
   - If `isMockMode('viettelpay')`, load `createPayment.json`, generate `transactionId` as `VTP_` + sha256 hash of `amount+orderInfo` (first 12 hex chars). Return `{ transactionId, paymentUrl: 'https://sandbox.viettelpay.vn/pay/' + transactionId, amount: args.amount, orderInfo: args.orderInfo, code: '00', message: 'Success', _mock: fixture._mock }`.
   - Else throw real API not implemented error.

2. `queryStatus(args: { transactionId: string })`:
   - If mock, load `queryStatus.json`, return `{ transactionId: args.transactionId, code: '00', message: 'Success', status: 'COMPLETED', amount: fixture.amount, _mock: fixture._mock }`.

3. `refund(args: { transactionId: string; amount: number; description?: string })`:
   - If mock, load `refund.json`, return `{ transactionId: args.transactionId, refundId: 'VTP_REFUND_' + Date.now(), amount: args.amount, code: '00', message: 'Refund successful', _mock: fixture._mock }`.

**Mock fixtures:**

`createPayment.json`:
```json
{ "transactionId": "VTP_placeholder", "paymentUrl": "https://sandbox.viettelpay.vn/pay/VTP_placeholder", "amount": 100000, "code": "00", "message": "Success", "_mock": true }
```

`queryStatus.json`:
```json
{ "transactionId": "VTP_placeholder", "status": "COMPLETED", "amount": 100000, "code": "00", "message": "Success", "_mock": true }
```

`refund.json`:
```json
{ "transactionId": "VTP_placeholder", "refundId": "VTP_REFUND_placeholder", "amount": 50000, "code": "00", "message": "Refund successful", "_mock": true }
```

`errorInsufficientBalance.json`:
```json
{ "code": "06", "message": "Insufficient balance", "_mock": true }
```
  </action>
  <verify>
    <automated>cd /Users/huynguyen/Desktop/vietnamese-mcp && npx tsc --build servers/mcp-viettel-pay/tsconfig.json --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - test -f servers/mcp-viettel-pay/MOCK_DEVIATIONS.md
    - grep -q 'SOAP' servers/mcp-viettel-pay/MOCK_DEVIATIONS.md
    - grep -q 'Auth scheme' servers/mcp-viettel-pay/MOCK_DEVIATIONS.md
    - grep -q '"@vn-mcp/mcp-viettel-pay"' servers/mcp-viettel-pay/package.json
    - grep -q 'VIETTELPAY_SANDBOX=true vitest run' servers/mcp-viettel-pay/package.json
    - grep -q 'VIETTEL_PAY_PARTNER_CODE' servers/mcp-viettel-pay/src/credentials.ts
    - grep -q 'signHmacSha256' servers/mcp-viettel-pay/src/signatures.ts
    - grep -q "isMockMode('viettelpay')" servers/mcp-viettel-pay/src/client.ts
    - grep -q '99999999' servers/mcp-viettel-pay/src/client.ts
    - grep -q '_mock' servers/mcp-viettel-pay/src/mock/createPayment.json
  </acceptance_criteria>
  <done>
    MOCK_DEVIATIONS.md written first with all field assumptions documented. Package scaffold compiles. Credentials load 3 env vars. Signatures wrap signHmacSha256 with assumption comment. Client has 3 mock methods with error trigger at amount=99999999. All 4 JSON fixtures exist.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement 3 ViettelPay tool handlers, server entry point, registerAll barrel, and integration tests</name>
  <files>
    servers/mcp-viettel-pay/src/tools/createPayment.ts,
    servers/mcp-viettel-pay/src/tools/queryStatus.ts,
    servers/mcp-viettel-pay/src/tools/refund.ts,
    servers/mcp-viettel-pay/src/tools/index.ts,
    servers/mcp-viettel-pay/src/index.ts,
    servers/mcp-viettel-pay/src/__tests__/integration.test.ts
  </files>
  <read_first>
    servers/mcp-viettel-pay/src/client.ts,
    servers/mcp-viettel-pay/src/credentials.ts,
    servers/mcp-viettel-pay/MOCK_DEVIATIONS.md,
    servers/mcp-momo-vn/src/tools/createPayment.ts,
    servers/mcp-momo-vn/src/tools/index.ts,
    servers/mcp-momo-vn/src/__tests__/integration.test.ts
  </read_first>
  <action>
**Tool handlers** — each follows the exact MoMo pattern.

**createPayment.ts:**
- Tool name: `viettel_pay_create_payment`
- Description: `Create a ViettelPay payment request. Returns a payment URL for the customer. Note: this is a mock-only server — see MOCK_DEVIATIONS.md for assumptions.`
- Zod schema: `amount: z.number().int().positive().describe('Payment amount in VND')`, `orderInfo: z.string().min(1).describe('Order description')`, `returnUrl: z.string().url().optional().describe('URL to redirect after payment')`
- Handler calls `viettelPayClient.createPayment(args)`.

**queryStatus.ts:**
- Tool name: `viettel_pay_query_status`
- Description: `Check ViettelPay transaction status by transactionId. Note: mock-only server.`
- Zod schema: `transactionId: z.string().min(1).describe('ViettelPay transaction ID (e.g., VTP_xxxx)')`
- Handler calls `viettelPayClient.queryStatus(args)`.

**refund.ts:**
- Tool name: `viettel_pay_refund`
- Description: `Refund a ViettelPay transaction. Note: mock-only server.`
- Zod schema: `transactionId: z.string().min(1).describe('Original transaction ID to refund')`, `amount: z.number().int().positive().describe('Refund amount in VND')`, `description: z.string().optional().describe('Reason for refund')`
- Handler calls `viettelPayClient.refund(args)`.

**tools/index.ts** — barrel importing 3 register functions, exporting `registerAll(server)`.

**index.ts** — McpServer name `mcp-viettel-pay`, version `0.0.1`. registerAll before connect.

**integration.test.ts:**
Set `process.env.VIETTELPAY_SANDBOX = 'true'` at top.

Tests:

1. `viettel_pay_create_payment returns paymentUrl with _mock:true (VTPAY-01)` — call with `{ amount: 100000, orderInfo: 'Test ViettelPay order' }`, expect `isError` falsy, `data._mock === true`, `data.transactionId` starts with `VTP_`, `data.paymentUrl` contains `viettelpay.vn`, `data.amount === 100000`, `data.code === '00'`.

2. `viettel_pay_create_payment with amount=99999999 returns isError:true (VTPAY-01 error)` — expect `result.isError === true`.

3. `viettel_pay_query_status returns status with _mock:true (VTPAY-02)` — call with `{ transactionId: 'VTP_abc123def456' }`, expect `data._mock === true`, `data.transactionId === 'VTP_abc123def456'`, `data.status === 'COMPLETED'`, `data.code === '00'`.

4. `viettel_pay_refund returns refund confirmation with _mock:true (VTPAY-03)` — call with `{ transactionId: 'VTP_abc123def456', amount: 50000 }`, expect `data._mock === true`, `data.amount === 50000`, `data.code === '00'`, `data.refundId` starts with `VTP_REFUND_`.

5. `viettel_pay_create_payment is deterministic (VTPAY-04)` — same inputs yield same transactionId.
  </action>
  <verify>
    <automated>cd /Users/huynguyen/Desktop/vietnamese-mcp && npm test --workspace=servers/mcp-viettel-pay 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'viettel_pay_create_payment' servers/mcp-viettel-pay/src/tools/createPayment.ts
    - grep -q 'viettel_pay_query_status' servers/mcp-viettel-pay/src/tools/queryStatus.ts
    - grep -q 'viettel_pay_refund' servers/mcp-viettel-pay/src/tools/refund.ts
    - grep -q 'registerAll' servers/mcp-viettel-pay/src/tools/index.ts
    - grep -q 'registerAll(server)' servers/mcp-viettel-pay/src/index.ts
    - grep -q 'VIETTELPAY_SANDBOX' servers/mcp-viettel-pay/src/__tests__/integration.test.ts
    - grep -q 'VTPAY-01' servers/mcp-viettel-pay/src/__tests__/integration.test.ts
    - grep -q 'VTPAY-03' servers/mcp-viettel-pay/src/__tests__/integration.test.ts
    - npm test --workspace=servers/mcp-viettel-pay exits with code 0
  </acceptance_criteria>
  <done>
    All 3 ViettelPay tools registered and callable via MCP. Integration test has 5 tests covering VTPAY-01 through VTPAY-04. `npm test --workspace=servers/mcp-viettel-pay` passes all tests.
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/huynguyen/Desktop/vietnamese-mcp && npm test --workspace=servers/mcp-viettel-pay` — all 5 tests pass
2. `npx tsc --build servers/mcp-viettel-pay/tsconfig.json --noEmit` — no type errors
3. `test -f servers/mcp-viettel-pay/MOCK_DEVIATIONS.md` — assumptions documented
4. `grep -c '|' servers/mcp-viettel-pay/MOCK_DEVIATIONS.md` — table has 13+ assumption rows
</verification>

<success_criteria>
- mcp-viettel-pay package exists with 3 tools: viettel_pay_create_payment, viettel_pay_query_status, viettel_pay_refund
- MOCK_DEVIATIONS.md documents all field assumptions with Source and Confidence columns
- All 5 integration tests pass in mock mode (VIETTELPAY_SANDBOX=true)
- Server structure mirrors mcp-momo-vn exactly (credentials, signatures, client, tools, mock, tests)
</success_criteria>

<output>
After completion, create `.planning/phases/04-zalo-oa-viettelpay-servers/04-02-SUMMARY.md`
</output>
