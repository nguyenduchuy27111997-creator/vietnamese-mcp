---
title: "ZaloPay Guide: Process Payments with Claude Code"
description: "Integrate ZaloPay into your app using the ZaloPay MCP server — create orders, check status, process refunds, and validate callback MAC signatures via Claude Code."
date: "2026-03-27"
author: "VN MCP Hub Team"
tags: ["guide", "zalopay", "payments", "mcp", "vietnam"]
---

ZaloPay is Vietnam's fast-growing payment platform, tightly integrated with the Zalo social ecosystem. The ZaloPay MCP server gives Claude Code direct access to the ZaloPay API — create payment orders, monitor transaction status, handle refunds, and verify callback authenticity without writing a line of API code yourself.

## Quick Setup

**Option 1 — npm (self-hosted):**

```bash
npm install -g @vn-mcp/mcp-zalopay-vn
```

Add to your `.mcp.json`:

```json
{
  "mcpServers": {
    "zalopay": {
      "command": "mcp-zalopay-vn",
      "env": {
        "ZALOPAY_SANDBOX": "true",
        "ZALOPAY_APP_ID": "2553",
        "ZALOPAY_KEY1": "PcY4iZIKFCIdgZvA6ueMcMHHUbRLYjPL",
        "ZALOPAY_KEY2": "kLtgPl8HHhfvMuDHPwKfgfsY4Ydm9eIz",
        "ZALOPAY_ENDPOINT": "https://sb-openapi.zalopay.vn"
      }
    }
  }
}
```

**Option 2 — Hosted gateway:**

```json
{
  "mcpServers": {
    "zalopay": {
      "url": "https://vn-mcp-gateway.nguyenduchuy27111997.workers.dev/mcp/zalopay"
    }
  }
}
```

## Available Tools

| Tool | Description |
|------|-------------|
| `zalopay_create_order` | Create a ZaloPay payment order. Returns a payment URL for the customer to complete payment. |
| `zalopay_query_order` | Check the status of a ZaloPay order by its `app_trans_id`. |
| `zalopay_refund` | Refund a ZaloPay transaction by its `zp_trans_id`. |
| `zalopay_validate_callback` | Validate a ZaloPay callback payload by verifying its HMAC-SHA256 MAC using key2. |

## Example Prompts for Claude

Type these directly into Claude Code after connecting the ZaloPay MCP server:

- "Create a ZaloPay order for 200,000 VND with description 'Monthly subscription'"
- "Check order status for app_trans_id 260327_abc123"
- "Refund transaction zp_trans_id ZP001 for 100,000 VND"
- "Validate this ZaloPay callback payload and confirm the MAC is correct: `{\"data\":\"...\",\"mac\":\"...\"}`"

## Common Workflows

**1. Create and monitor an order:**
1. Call `zalopay_create_order` with `amount` and `description` — receive `order_url`
2. Share the `order_url` with the customer to complete payment
3. Call `zalopay_query_order` with the `app_trans_id` — confirm `return_code: 1` (success)

**2. Validate a ZaloPay callback:**
1. Receive raw JSON body from ZaloPay callback webhook
2. Call `zalopay_validate_callback` with the raw body as a string
3. Check `valid: true` before processing the payment event — MAC is verified against key2

**3. Refund a transaction:**
1. Call `zalopay_query_order` to get the `zp_trans_id` from a successful order
2. Call `zalopay_refund` with `zpTransId` and the refund amount

## Environment Variables

| Variable | Description | Mock Fallback |
|----------|-------------|---------------|
| `ZALOPAY_SANDBOX` | Enable mock mode (set to `true`) | `true` |
| `ZALOPAY_APP_ID` | ZaloPay app ID | `2553` |
| `ZALOPAY_KEY1` | Key1 for request signing | `PcY4iZIKFCIdgZvA6ueMcMHHUbRLYjPL` |
| `ZALOPAY_KEY2` | Key2 for callback MAC verification | `kLtgPl8HHhfvMuDHPwKfgfsY4Ydm9eIz` |
| `ZALOPAY_ENDPOINT` | ZaloPay API endpoint | `https://sb-openapi.zalopay.vn` |

<Note>The fallback values above are ZaloPay's public sandbox credentials — safe for development and testing.</Note>

## Next Steps

- Full tool reference with all parameters and response schemas: [ZaloPay Server Reference](/servers/zalopay)
- See all 5 servers in action: [Introducing VN MCP Hub](/blog/launch)
- Source code and issues: [GitHub — mcp-zalopay-vn](https://github.com/nguyenduchuy27111997/vietnamese-mcp/tree/main/servers/mcp-zalopay-vn)
